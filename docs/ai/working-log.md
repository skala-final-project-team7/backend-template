# Working Log

> 작업 완료 후 핵심 변경 사항과 결정을 일자별로 누적 기록한다.
> 각 모듈의 진행 중 Plan(체크리스트)은 `backend/<module>/current-plans.md` 를 참조한다.

---

## 2026-06-16

- **auth-server Feature 6 + `/internal/**` 인증 차단 라이브 스모크 — 실 Atlassian admin-key activate/deactivate 실측 통과**: 06-15 Feature 6 구현·admin seed 위에서, 실제 Atlassian Premium 사이트에 대고 내부 API 를 수동 스모크했다. **환경**: 로컬 `:auth-server:bootRun`(docker MySQL) + admin seed(`LINA_ADMIN_*` env 주입) + 임시 JWT/암호화 키 + `INTERNAL_API_KEY='smoke-internal-key'`. **WireMock 아님 — 진짜 Atlassian 호출**. 단 임시 키·localhost 전제라 이는 **"기능 동작 확인"이지 "운영 검증"이 아니다**(운영 키·실 배포 검증과 구분). **결과(전부 통과)**: ① **인증 차단** — `/internal/**` 키 없음·위조 키 → `401`, 올바른 `X-Internal-Api-Key` → 통과. `InternalApiKeyFilter` 실동작 확인. ② **activate** — `POST /internal/admin/key/activate {adminUserId, jobId}` → `200 {"expirationTime":"2026-06-15T09:50:39.742Z"}`. auth-server 가 `admin_atlassian_credential`(site_url+API Token) 로드 → Atlassian `POST {siteUrl}/wiki/api/v2/admin-key`(Basic auth) 호출 → 실제 admin key 활성, 만료 ~1시간 후 = **`durationInMinutes=60` 동작 확인**. `200` 자체가 **admin seed 정상**(accountId 로 admin·credential 조회 성공 — 미적재였으면 `404`)임을 동시 입증. ③ **deactivate** — `POST /internal/admin/key/deactivate {adminUserId, jobId}` → `200 Content-Length:0`. Atlassian `DELETE /wiki/api/v2/admin-key` 로 키 원복(Atlassian 자체는 `204` 지만 auth-server 가 `toBodilessEntity` 로 `200` 반환 — 계약대로). **activate→deactivate 한 사이클을 실 사이트에서 검증**. 앞서 자동 테스트 131건 통과(06-15) 위에서 수행. **변경 파일 없음**(검증만 — 코드/문서 변경 없음). **잔여(Feature 5 ⏳ 진행 중)**: credential 조회 라이브 스모크는 admin OAuth 로그인이 필요한데(`user_tokens` 가 로그인 산물이라 seed 불가) — ⓐ 초기 `TOO_MANY_REDIRECTS` 는 OAuth env 미주입(`CONFLUENCE_AUTHORIZATION_URI`/`REDIRECT_URI`/`SCOPES` 빈 값으로 authorize URL 이 호스트 없는 상대경로 → 자기 자신 루프)이 원인 — `oauth.env` 채워 해소(login `Location` 이 완전한 `https://auth.atlassian.com/authorize?...` 로 정상화 실측), ⓑ 현재 dev console **콜백 URL 미등록/불일치**(redirect_uri mismatch)에서 막힘 — `http://localhost:8081/api/auth/callback` 을 dev console 에 등록해야 동의 화면 진입. 등록 후엔 **admin.env accountId 본인 계정**으로 로그인해야 `user_tokens` 가 맞는 행에 생겨 credential 조회 `200`(다른 계정이면 `404`). **주의**: 스모크 중 OAuth client secret 이 셸/대화에 노출될 수 있음 — dev console secret rotate 권고(06-12 스모크와 동일 주의).

## 2026-06-15

- **auth-server 3단계 Feature 6 완료 — Admin Key 내부 API(`/internal/admin/key/{activate,deactivate}`)**: BFF admin ingest 플로우 전용 내부 API 를 구현했다. **activate**: `role==ADMIN` 검증 → `admin_atlassian_credential`(site_url+API Token, §6.4) 로드 → Atlassian `POST {siteUrl}/wiki/api/v2/admin-key`(`HttpHeaders.setBasicAuth(email, token)` = base64, body `{durationInMinutes}`) 호출, `expirationTime` 반환. **`durationInMinutes=60`(최대값)** 외부화(`${ADMIN_KEY_DURATION_MINUTES:60}`) — 10분 기본값이면 장기 수집 중 키 만료로 제한 페이지 404 누락(Feature 0 실측). 미활성 확인 없이 **반복 호출 안전**(BFF 가 ingest 마다 호출). **deactivate**: Atlassian `DELETE` → `toBodilessEntity`, **`jobId` 기준 idempotent**(in-memory TTL store, ConcurrentHashMap — 중복 completion event 는 두 번째 DELETE 없이 2xx, 4xx 금지로 BFF DLQ 오작동 방지). **호출 주체 제한**: `SecurityConfig` 에 `/internal/admin/**` 을 Feature 5 의 `InternalApiKeyFilter`(ROLE_INTERNAL)로 개방, 잔여 `/internal/**` denyAll 유지. **에러**: 누락 400(@Valid) / 비ADMIN 403 / credential 없음 404 / Atlassian 장애 502(`AdminKeyException`→`BizException` 매핑). `AdminKeyClient` 는 BFF `AuthAdminKeyClient`(auth-server 호출자)와 역할 다름 — auth-server 가 Atlassian 을 호출하는 쪽. 검증: 실패 테스트 선작성(신규 26건 red→green — Client WireMock 4·Service 단위 11·Controller MockMvc 11, 내부키 없음 401·위조키 401·사용자 JWT 403 차단 포함) 후 구현, `:auth-server:test` 127건 통과, `./scripts/verify.sh` 통과. **실 Atlassian 미호출(WireMock)** — 진짜 admin key 켜고 끄기는 4단계 통합. 변경 파일: `internal/{AdminKeyController,AdminKeyService,AdminKeyClient}.java`(신규), `internal/dto/{AdminKeyRequest,AdminKeyActivateResponse}.java`(신규), `config/SecurityConfig.java`, `application*.yml`, 테스트 3종(신규), `current-plans.md`(Feature 6 체크리스트 7개 `[x]`, 통합 테스트 1개는 4단계로 유지).
- **auth-server admin seed 구현 — 로그인 전 사전 주입(`AdminSeeder`)**: F5/F6 가 `admin_atlassian_credential` 행이 없으면 404 로 막히던 갭을 실제로 해소. **`AdminSeeder`(`ApplicationRunner`)**가 부팅 시 `lina.admin-seed.*` 로 `users`(role=ADMIN, `access_token='ADMIN_PLACEHOLDER'`) + `admin_atlassian_credential`(site_url + 평문 api-token→저장 시 `TokenCipher` converter 가 암호화) **멱등 INSERT**(행 있으면 skip, 4개 중 하나라도 비면 skip). `user_tokens`(OAuth)는 로그인 산물이라 seed 대상 아님 → F6 은 seed 만으로, F5 는 admin 1회 로그인 후 동작. **설정원 = 전부 env 주입(`lina.admin-seed.*`)**: accountId/email/name/site_url/api-token 모두 env(k8s Secret/ConfigMap) — admin 식별정보를 git 에 안 남기고 한 묶음으로 관리, api-token 은 secret. (논의 경과: 처음 env→"고정값인데 왜 env냐"는 의견으로 yml 평문 기입으로 바꿨다가, 둘이 코드/DB 결과는 동일하고 차이는 'git 노출 vs 주입 단순'뿐 → admin 식별정보 git 미노출·환경 유연·한 경로 일관 이점으로 **전부 env 로 확정**.) seeder 를 쓰는 이유는 "환경마다 다름"이 아니라 **`admin_atlassian_credential` 이 site_url+api_token_enc 한 행인데 api_token 이 암호화 컬럼이라 Flyway 평문 SQL 로 그 행을 못 만들기 때문**(seeder 불가피). users 도 같은 seeder 로 통일. 검증: 실패 테스트 선작성(4건 — 미설정 skip·신규 user+credential INSERT·기존 user 보존·완전 멱등) 후 구현, `:auth-server:test` **131건** 통과(contextLoads 포함 — ApplicationRunner 정상), `verify.sh` 통과. 변경 파일: `token/{AdminSeeder,AdminSeedProperties}.java`(신규)·`token/AdminSeederTest.java`(신규)·`application.yml`(`lina.admin-seed.*` 전부 env)·`docs/db-schema.md` §6.1·`current-plans.md`. **잔여**: `LINA_ADMIN_*` 5개 env(accountId/email/name/site-url/api-token) 주입 후 bootRun 스모크(Feature 8). bff 6파일은 verify 의 spotlessApply 가 또 딸려와 되돌림(origin/main 정리본 미머지 — 담당 외).

- **admin seed 트랜잭션 경계 분리 리팩토링 (`AdminSeedService`)**: `ApplicationRunner`의 `run()`은 시드 로직을 직접 호출해 `seed()`의 `@Transactional`이 적용되지 않을 수 있던 문제를 제거하기 위해 `AdminSeedService`로 시드 본문을 분리. 이제 `AdminSeeder.run()`은 `adminSeedService.seed()`를 위임 호출만 수행하고, 트랜잭션은 `AdminSeedService.seed()`에서 안전하게 보장. 동작은 동일하게 멱등성 유지하고, `AdminSeederTest`는 서비스 기반(`AdminSeedService`)으로 검증 정리. 검증: `:auth-server:test --tests "com.lina.auth.token.AdminSeederTest"` 재실행.
- **Feature 5·6 plan 을 RabbitMQ Provisioning 노트(2026-06-14) 기반으로 정합화**: 팀 인프라 PDF(BFF↔RabbitMQ↔Data Ingestion Worker 전체 비동기 파이프라인)를 읽고 auth-server 두 내부 API 의 호출 위치를 확정했다. `backend/auth-server/current-plans.md` 에 **§Feature 5·6 공통 흐름도** 신설(① BFF jobId 생성 → ② activate(F6) → ③ RabbitMQ publish → ④ Worker `credential_lookup`(F5) → ⑤ 수집 → ⑥ completion event → ⑦ deactivate(F6)). **PDF 대조로 확정**: 1.6.b(credential 조회)=F5 코드 완료, 1.4(activate 시 Admin Key 발급 절차)=F6 핵심 미구현, 전체 외부연동 통합테스트=4단계. **정합성 발견(F5 에 기록)**: Worker `credential_lookup` 은 `(access_token, cloud_id)` **2-tuple 만 소비** → 우리 응답 4필드 중 accessToken/cloudId 만 사용(siteUrl/expiresAt 현 미소비, 계약 유지 — 불일치 아님), Worker 가 lookup 실패 시 fallback(PoC 에선 None→수집 실패)이라 F5 의 401/404/502 는 Worker 로그에서만 드러남. RabbitMQ exchange/queue/DLQ 프로비저닝은 Helm+ArgoCD GitOps(Infra) 소관 — auth-server 범위 밖(흐름도 참조만). F6 변경대상에 `SecurityConfig`(`/internal/admin/**` ROLE_INTERNAL 확장)·`RestClientConfig` 추가.
- **Feature 5·6 외 문서 정합성 점검·정정(이번 작업이 건드린 계약의 파급)**: Feature 5 구현 + `X-Internal-Api-Key` 신규 도입이 다른 문서와 어긋난 곳을 정리. (1) auth-server plan **진행 상태 요약** "착수 가능"→"Feature 0~5 구현 완료, 6~8 잔여"로 갱신(stale 해소), (2) **Feature 7** `/internal/**` 화이트리스트 항목이 F5 에서 이미 도입한 `InternalApiKeyFilter` 를 모른 채 "처음 하는 일"처럼 돼 있던 것 → "응용계층 인증은 F5/F6 완료, F7 은 NetworkPolicy 병행"으로 정합, (3) **위험 요소 표** 2건 추가(admin credential 등록경로 부재·`X-Internal-Api-Key` Worker 미합의 — 각각 404·401 로 실연동 차단), (4) **`docs/api-spec.md` 변경 이력 v2.7.0** 추가(§2-5 에 `X-Internal-Api-Key` 헤더 추가했는데 버전이력 누락 — 문서 컨벤션), (5) **`docs/db-schema.md` §6.4** 에 등록 경로 노트 추가(테이블 정의만 있고 누가 행을 넣는지 부재).
- **admin seed 설계 확정 — 로그인 전 사전 seed (plan·db-schema 반영)**: `admin_atlassian_credential` 행을 넣을 수단이 없어 F5/F6 가 모두 404 로 막히는 갭을 해소. **데이터별 주입 방법 확정**: ① `users`(role=ADMIN/email/accountId)+`admin_atlassian_credential.site_url` = **Flyway 평문 seed**, ② `admin_api_token_enc`(AES-GCM 컬럼, 평문 SQL 불가·암호문 git 금지) = **앱 기동 seeder**(`ApplicationRunner` 가 env(k8s Secret) 평문 토큰을 `TokenCipher` 암호화 후 행 없으면 INSERT), ③ `user_tokens`(OAuth) = **로그인 산물**(사전 seed 아님). 결과: **F6(admin-key)은 사전 seed 만으로 동작, F5(credential 조회)는 admin 1회 로그인 후 동작**. `users.access_token` 은 NOT NULL 이라 seed 시 **`'ADMIN_PLACEHOLDER'` 더미값으로 INSERT**(스키마 변경 없이 유지, 첫 로그인 시 진짜 JWT 로 덮어씀 — nullable 대신 placeholder 선택). 구현(seed 마이그레이션 + seeder)은 별도 후속 작업. 변경 파일: `backend/auth-server/current-plans.md`(진행상태·Feature 1·위험요소·확정된 결정), `docs/db-schema.md` §6.1·§6.4. **코드 변경 없음(문서 확정만).**

## 2026-06-15

- **관리자 대시보드 `spaceId` 중복 카운트 메모리 부담 완화**: `AdminDataMongoRepository.countDistinctNonNull()`에서 `findDistinct + stream count` 방식으로 결과 전체를 JVM으로 가져오던 구현을 MongoDB 집계 파이프라인(`$match` → `$group` → `$count`)으로 변경해 서버 메모리 사용을 줄였다.
  - `count != null/빈 문자열` 조건은 `buildNonNullAndNonEmptyCriteria`로 재사용하고, 집계 결과의 `count`만 받아와 반환한다.
  - 변경 파일: `backend/bff-server/src/main/java/com/lina/bff/admin/dashboard/repository/AdminDataMongoRepository.java`
  - 테스트 변경 파일: `backend/bff-server/src/test/java/com/lina/bff/admin/dashboard/repository/AdminDataMongoRepositoryTest.java` (`findDistinct` 목업 제거, `aggregate` 결과 스텁 기반 확인, 결과 0 케이스 보강)
  - 검증: `cd backend && ./gradlew test` (`BUILD SUCCESSFUL`)

- **관리자 대시보드 sync_logs 마지막 동기화 시각 파싱 안정화**: `AdminDataMongoRepository.toInstant(Object)`가 문자열 시간을 `Instant.parse`만 시도하던 방식으로 인해 형식 불일치 시 `DateTimeParseException`이 상위로 전파되던 부분을 보완했다.
  - `parseStringToInstant()`를 도입해 `Instant.parse` 실패 시 `OffsetDateTime.parse` → `ZonedDateTime.parse` 순으로 폴백하고, 최종 실패 시 `null`로 처리해 `/api/admin/data`에서 집계값 계산이 500으로 단락되지 않도록 했다.
  - 테스트 파일에서 잘못된 시간 문자열 입력이 있어도 예외 없이 유효한 다음 필드 값으로 fallback 하거나 `null`을 안전하게 반환하는 동작을 확인하는 단위 테스트를 추가했다.
  - 변경 파일: `backend/bff-server/src/main/java/com/lina/bff/admin/dashboard/repository/AdminDataMongoRepository.java`, `backend/bff-server/src/test/java/com/lina/bff/admin/dashboard/repository/AdminDataMongoRepositoryTest.java`
  - 검증: `cd backend && ./gradlew test` (`BUILD SUCCESSFUL`)

- **관리자 대시보드 `spaceId` 집계 null/빈 문자열 제외 처리 강화**: `countDistinctSpaces()`의 공간 식별자 집계를 위해 `Criteria.where(field).ne(null)`만으로는 빈 문자열을 걸러내지 못하던 문제를 해결했다. `AdminDataMongoRepository`에서 `buildNonNullAndNonEmptyQuery()`를 도입해 null/빈 문자열을 모두 필터링하고, `countDistinctNonNull()` 및 `findLatestInstantByField()`에 공통 적용했다.
  - 변경 파일: `backend/bff-server/src/main/java/com/lina/bff/admin/dashboard/repository/AdminDataMongoRepository.java`
  - 검증: `cd backend && ./gradlew test` (`BUILD SUCCESSFUL`)

- **관리자 사용자 현황 API 생성자 의존성 주입 리팩터링**: `AdminUsersService`와 연결된 `AdminUsersController`에서 생성자 코드를 제거하고 Lombok `@RequiredArgsConstructor`로 통일했다.
  - 변경 파일:
    - `backend/bff-server/src/main/java/com/lina/bff/admin/dashboard/service/AdminUsersService.java`
    - `backend/bff-server/src/main/java/com/lina/bff/admin/dashboard/controller/AdminUsersController.java`
  - 검증: `backend/`에서 `./gradlew test` 실행 (`BUILD SUCCESSFUL`).

- **`conversations` 사용자 집계 쿼리용 인덱스 추가**: 관리자 사용자 현황 집계에서 `userId IN (...)` + `deletedAt == null` 패턴을 더 직접적으로 커버하도록 MongoDB 인덱스를 추가했다.
  - 변경 파일:
    - `backend/bff-server/src/main/java/com/lina/bff/chat/entity/Conversation.java`
    - `docs/db-schema.md`
  - 추가 인덱스: `idx_conversations_user_active` (`{ userId:1, deletedAt:1 }`)  
    목적: `AdminUserMongoRepository.countActiveConversationsByUserIds(...)`에서 사용자별 활성 대화 수 집계를 위한 `$match` 선별 인덱스 보완.

- **관리자 사용자 수 조회 `$in` 배치 분할 튜닝**: `AdminUserMongoRepository.countActiveConversationsByUserIds(...)`에서 `userIds`를 `distinct` 처리 후 500개 단위로 쪼개 `$in` 조회를 수행하도록 변경했다.  
  - 단일 쿼리의 `$in` 과부하를 줄이고, 요청 데이터 급증 시 MongoDB 쿼리 오버헤드/타임아웃 리스크를 완화하기 위한 사전 튜닝이다.  
  - 중복 userId는 제거 후 집계하며, 각 배치 결과는 `merge`로 병합해 중복 누적에도 안전하게 처리한다.  
  - 운영 가이드: userIds 입력이 매우 클 경우에도 배치 크기 상한 유지가 성능과 안정성 균형에 유리.
  - 변경 파일: `backend/bff-server/src/main/java/com/lina/bff/admin/dashboard/repository/AdminUserMongoRepository.java`

- **관리자 대시보드/인입 API 컨트롤러 응답 형식 확장성 반영**: `ApiResponse` 래퍼 형식은 유지하면서 반환 타입을 `ResponseEntity<ApiResponse<T>>`로 통일했다. 변경 대상은 관리자 대시보드 전용 컨트롤러 5개(`AdminDataController`, `AdminUsersController`, `AdminStatsController`, `AdminFeedbackController`, `AdminSyncController`)와 관리자 수집 API 컨트롤러 `AdminIngestController`이다. 각 핸들러는 `ResponseEntity.ok(...)`로 감싸서 성공 응답을 반환한다.
  - 수정 파일:
    - `backend/bff-server/src/main/java/com/lina/bff/admin/controller/AdminIngestController.java`
    - `backend/bff-server/src/main/java/com/lina/bff/admin/dashboard/controller/AdminDataController.java`
    - `backend/bff-server/src/main/java/com/lina/bff/admin/dashboard/controller/AdminStatsController.java`
    - `backend/bff-server/src/main/java/com/lina/bff/admin/dashboard/controller/AdminUsersController.java`
    - `backend/bff-server/src/main/java/com/lina/bff/admin/dashboard/controller/AdminFeedbackController.java`
    - `backend/bff-server/src/main/java/com/lina/bff/admin/dashboard/controller/AdminSyncController.java`
- **검증**: `./gradlew test` (실행 위치: `backend/`) 성공.

## 2026-06-12

- **auth-server 3단계 Feature 5 완료 — 내부 credential 조회 API(`GET /internal/auth/admin-confluence-credential`)**: Data Ingestion Worker 전용 내부 API 를 구현했다. **동작**: `adminUserId`(accountId) → `users.role==ADMIN` 검증 → `user_tokens`(OAuth accessToken/cloudId) + `admin_atlassian_credential`(site_url) 로드 → access token 만료/임박(skew 60초) 시 AUTH-03 refresh → `{accessToken, cloudId, siteUrl, expiresAt(KST)}` **raw JSON**(공통 Wrapper 미적용, api-spec §2-5) 반환. `refreshToken`·admin API Token 은 응답 DTO 에 **필드 자체 없음**(구조적 미노출). refresh 는 외부호출(트랜잭션 밖) → DB 갱신만 `TransactionTemplate`(rotating 덮어쓰기) — `SessionService`/`OAuthLoginService` 와 동일 트랜잭션 분리 원칙(LINA 세션 담당 SessionService 재사용 대신 `InternalCredentialService` 별도 — Confluence 토큰과 LINA 세션은 다른 토큰). **호출 주체 제한**: 신규 `InternalApiKeyFilter` 가 `X-Internal-Api-Key` 헤더를 env(`${INTERNAL_API_KEY}`)와 **상수시간 비교**, 키 미설정 시 **fail-closed**(전부 거부). `/internal/auth/**` 만 `ROLE_INTERNAL` 개방, 사용자 JWT 는 403, 잔여 `/internal/**` 는 denyAll 유지(F6 미개방). **AUTH-03**: `AtlassianOAuthClient.refreshAccessToken` 추가, `invalid_grant` 는 `InvalidGrantException` 으로 구분 → 재로그인 필요 `401`, 그 외 일시장애 `502`. `TokenExchangeRequest` 에 `refreshToken` 필드+`@JsonInclude(NON_NULL)`(grant 별 미사용 필드 와이어 제외, 기존 AUTH-02 계약 불변). **에러 매핑**: 누락 `400`(@Validated+@NotBlank) / 비ADMIN `403` / 사용자·credential 없음 `404` / invalid_grant `401` / Atlassian 장애 `502`. 검증: 실패 테스트 선작성(신규 24건 red→green — MockMvc 11: **내부키 없는 외부호출 401·위조키 401·사용자 Bearer 403 전부 토큰 미반환** 포함, Service 단위 10, AUTH-03 WireMock 3) 후 구현, `:auth-server:test` 101건 통과, `./scripts/verify.sh` 통과. **실 Atlassian 미호출(전부 WireMock/MockMvc)** — 실 Worker 연동(X-Internal-Api-Key 합의·admin seed)은 4단계 통합. `docs/api-spec.md` §2-5 에 `X-Internal-Api-Key` 헤더 계약 반영. 변경 파일: `internal/{InternalCredentialController,InternalCredentialService}.java`(신규), `internal/dto/AdminConfluenceCredentialResponse.java`(신규), `config/InternalApiKeyFilter.java`(신규), `config/SecurityConfig.java`, `oauth/AtlassianOAuthClient.java`(AUTH-03), `oauth/dto/TokenExchangeRequest.java`, `application*.yml`, 테스트 2종(신규)+`AtlassianOAuthClientTest`, `docs/api-spec.md` §2-5, `backend/auth-server/current-plans.md`(Feature 5 `[x]`).

- **data-ingestion ingest job DLQ 정책 반영 및 검증 완료 (`featureI-7c Step 10`)**: ingest job 큐에 DLQ 선언/재시도 제어를 적용하고 실패 핸들링 정책 테스트를 추가한 뒤 검증까지 완료했다. `app/config.py`에 `ingest_job_dlq` 기본값을 `lina.data-ingestion.ingest.dlq`로 추가하고, `app/ingestion/bootstrap.py`에서 `ingest_job_queue` 선언 시 `durable` + `x-dead-letter-exchange: ""` + `x-dead-letter-routing-key: settings.ingest_job_dlq`를 설정했으며, `ingest_job_dlq` durable 큐 선언을 추가했다. `app/ingestion/workers/ingest_job_main.py`는 실패 시 `auto_ack=False`에서 처리하도록 정합해, 파싱/비즈니스 예외 시 `basic_reject(requeue=False)` 또는 `x-retry-count` 기반(1000/2000/4000/8000/10000ms) 재시도 후 재발행·재시도 소진 시 DLQ로 전환되도록 수정했으며, 파싱/재시도/실패/성공 동작을 테스트 가능한 형태로 정비했다. `app/ingestion/workers/ingestion_worker.py`의 기존 멱등성 가드(`IN_PROGRESS/COMPLETED/FAILED` skip)는 유지했다. 테스트 추가/수정: `tests/ingestion/test_bootstrap.py`(기본값·오버라이드 검증), `tests/ingestion/test_ingest_job_main.py`(retry header 파싱, malformed 메시지 처리, 재시도/재발행, DLQ 진입, 소비 루프 커버) — `python3.11 -m pytest tests/ingestion/test_ingest_job_main.py tests/ingestion/test_bootstrap.py`에서 `18 passed, 1 skipped`, `python3.11 -m pytest`에서 `238 passed, 2 skipped` 통과.

- **Admin dashboard 평균 응답 시간 집계 효율화**: `backend/bff-server/src/main/java/com/lina/bff/admin/dashboard/service/AdminStatsService.java`의 `averageResponseTimeSeconds()`에서 `List<Long>`로 응답 시간을 모두 적재한 뒤 평균을 계산하던 방식을, 합계(`responseTimeSum`)와 카운트(`responseTimeCount`) 누적 집계로 변경해 객체 생성과 후처리 비용을 줄였다. 테스트: `:bff-server:test --tests "com.lina.bff.admin.dashboard.service.AdminStatsServiceTest"` 통과, `./gradlew test` 통과.
- **Admin dashboard 통계 NPE 방지**: `backend/bff-server/src/main/java/com/lina/bff/admin/dashboard/service/AdminStatsService.java`에서 `hourlyAccessTrend()`가 `Message.getCreatedAt()` null 값을 방어하지 않아 생길 수 있는 `NullPointerException`을 방지하도록 `createdAt == null` 항목을 필터링해 건너뛰도록 수정했다. 테스트: `:bff-server:test --tests "com.lina.bff.admin.dashboard.service.AdminStatsServiceTest"` 통과, `./gradlew test` 통과.

- **auth-server 실제 Atlassian 3LO 라이브 스모크 완주(users/me 제외) — Feature 8 일부 선행**: Feature 3·4 완료 직후, Feature 8 게이트로 미뤄져 있던 검증 중 전제조건이 갖춰진 것(실 MySQL 부팅 + login~logout 사이클)을 선행 수행했다. 환경: dev console 3LO 앱 등록(classic 6종 + `read:me`), 로컬 docker MySQL 8.4, 키 전부 env 주입. **결과(전부 통과)**: ① bootRun — Flyway V001~V004 적용 + `ddl-auto: validate` 통과(plan 위험 요소 'Flyway 미검증' 해소), ② login 302 → Atlassian 동의 → callback `200`(JWT claim 실측: `userId`=accountId, `groups`=groupId 3건, `role`=USER, camelCase), ③ refresh 회전 `200` + **이전 refresh 재사용 `401`**, ④ logout `200`(`data:null`) + **logout 후 refresh `401`**, ⑤ Bearer 누락 logout `401`·만료 code `401`·위조 state `400`, ⑥ DB 실측 — `users` 1행(accountId/email/USER), `user_tokens` 암호문(HEX 검사, 평문 아님), `user_groups` 3건. **스모크가 잡은 결함 2건**(WireMock 으론 불가한 간극): `read:me` scope 누락(`/me` 403)·토큰 컬럼 2048 truncation — 각각 별도 항목으로 수정 기록. 진단 과정 부산물: Atlassian 직접 교환·accessible-resources 격리 테스트 기법, GlobalExceptionHandler 가 BizException cause 를 로그에 안 남겨 Atlassian 호출 실패 원인 추적이 어려운 점(**Feature 7 에서 실패 단계·HTTP status warn 로깅 보강 검토**). 잔여: users/me 는 BFF 측 작업 후 전체 사이클 재확인(plan Feature 8 항목에 부분 완료로 주석). 후속 조치: 스모크 중 client secret 이 대화/셸에 노출 — **dev console 에서 secret rotate 필요**.
- **`user_tokens` 토큰 암호화 컬럼 2048→8192 확장 — 라이브 스모크 실측 결함 수정**: Feature 3 callback 라이브 스모크에서 Confluence 토큰 저장이 `Data too long for column 'confluence_access_token_enc'`(MySQL 1406) 로 실패 — **실제 Atlassian access token 은 2KB 초과 JWT — 실측 2,561자**(암호화 후 2,589B > 2,048B, TokenCipher 저장 포맷 = 원문 + IV 12B + GCM 태그 16B)인데 `VARBINARY(2048)` 로 산정돼 있었다. 8192 = 실측 대비 ~3.2배 여유. WireMock 테스트는 `"conf-access"` 류 짧은 가짜 토큰이라 잡을 수 없던 간극. 재현 테스트 선작성(`UserTokenRepositoryTest.shouldPersistRealWorldSizedTokens` — 4,800자 토큰 라운드트립, H2 에서도 동일 truncation 재현 red 확인) 후 `V003`/`UserToken` Entity 의 access·refresh 두 컬럼을 `VARBINARY(8192)` 로 확장, `docs/db-schema.md` §6.2 동반 갱신. **V003 직접 수정**(미배포 단계 — V001 전례와 동일 기준)이라 기존 로컬 DB 는 체크섬 불일치 → 컨테이너 재생성 필요. 검증: 재현 테스트 red→green, `:auth-server:test` 전체 통과, `./scripts/verify.sh` 통과. 변경 파일: `db/migration/V003__create_user_tokens.sql`, `token/entity/UserToken.java`, `token/repository/UserTokenRepositoryTest.java`, `docs/db-schema.md` §6.2.
- **OAuth scope 셋 classic 통일 정정(granular 1개 제거) — 수동 스모크 준비 중 발견, 2차 검증으로 재정정**: Atlassian dev console 앱 등록(Feature 8 선행 수동 스모크 준비) 중, 06-10 확정 scope 셋에 dev console 미등록 granular 가 섞여 authorize 요청이 거부되는 문제 확인 — plan 의 "classic·granular 혼용은 dev console 등록 시 재확인" 단서가 현실화된 것. 공식 scope 표·operation 문서 실측(2차에 걸쳐 검증, 1차 결론 일부 정정): ① `readonly:content.attachment:confluence` 는 형태(`…:confluence`)는 granular 같지만 **Classic scopes 표 등재**("Download attachments…") — 형태 기반 분류의 예외, **유지**(1차에서 granular 로 오판해 제거 지시했던 것 정정). ② 우리 셋의 유일한 granular 는 **`read:content.restriction:confluence`** 뿐 — 이것만 제거. ③ v1 restriction 조회(`GET /wiki/rest/api/content/{id}/restriction`) operation 문서의 classic scope 는 **`read:confluence-content.all`** → restriction scope 제거에도 기능 손실 없음(classic `read:confluence-content.permission` 대체설은 근거 없어 기각, granular 등가물 이름도 `read:content-details:confluence` — 기존 plan 의 granular 표기는 오기). ④ AUTH-05 memberof 의 classic 은 `read:confluence-user` 단독으로 충분(operation 문서). **최종 scope(classic 6종)**: `read:confluence-content.all readonly:content.attachment:confluence read:confluence-user read:confluence-groups read:confluence-space.summary offline_access`. **전제/전파**: classic 통일 = 콘텐츠 조회 v1 API 전제 — Feature 5 가 이 OAuth 토큰을 Data Ingestion Worker 에 전달하므로 수집측은 실제 호출 operation 목록 확정 후 REST 문서의 operation 별 scope 행으로 최종 대조 필요(공식 방법론, v2 사용 시 granular 별도 등록). 코드 변경 없음(`CONFLUENCE_SCOPES` env 주입값). 변경 파일: `backend/auth-server/current-plans.md`(§구현 시 주의 'scope 충분성' 정정), `docs/ai/working-log.md`. **[추가 발견 — 같은 날 라이브 스모크]** Feature 3 callback 의 user-info `/me` 호출이 실 Atlassian 에서 **403** — `/me` 는 Confluence 가 아닌 **User Identity API** 라 **`read:me` scope 필요**(Confluence scope 만으론 거부). WireMock 테스트는 `/me` 를 성공으로 모킹해 잡을 수 없던 간극으로, 라이브 스모크 격리 테스트(직접 교환 → ② accessible-resources 200 / ③ `/me` 403)로 특정. 조치: dev console 에 User Identity API(`read:me`) 추가 등록 + scope 셋에 `read:me` 포함(최종 7종). 부수 확인: AUTH-02 직접 교환·AUTH-04 accessible-resources 는 실 Atlassian 실측 정상(cloudId·scope 부여 확인), 만료 code 재사용 401·미발급 state 400 매핑도 실측 정상.

- **auth-server `SessionService` 트랜잭션 범위 축소 및 테스트 반영**: `refresh`에서 JWT 검증/토큰 발급 전처리를 트랜잭션 밖으로 두고, DB 갱신 핵심(사용자 조회-토큰 대조-회전-save)을 `TransactionTemplate` 블록으로 분리했다 (`PlatformTransactionManager` 주입, `SessionService.rotateSession` 적용). `SessionServiceTest`도 신규 생성자 시그니처(`PlatformTransactionManager`)에 맞게 수정했고, 변경 후 `:auth-server:test`와 루트 `test`가 모두 통과되었다.

- **auth-server `OAuthLoginService` 트랜잭션 분리 리팩토링 — 이전/개선 상태 정리**: 이전에는 `handleCallback`이 `@Transactional` 한 개의 큰 트랜잭션 안에서 Atlassian OAuth 외부 호출(코드 교환·사이트 조회·사용자정보·groups 조회) + DB 영속(upsert/user_groups/user_tokens)을 같이 수행해, 응답 지연 시 DB 락 구간이 불필요하게 길어지고(재시도/차단 이슈), 클래스 final 여부·프록시 정책 변경까지 유발되는 구조였다. 현재는 `OAuthLoginService`에서 외부 호출과 검증/토큰 발급만 수행하고, `OAuthLoginPersistenceService`를 새로 두어 영속만 `@Transactional`로 처리한다. 이로써 `handleCallback`은 I/O 실패와 영속성 실패를 분리해 판단할 수 있고, DB 작업은 짧은 단일 단위로 축소됐다. 실행 결과: `OAuthLoginServiceTest`(callback 단위) 및 `:auth-server:test` 전체 테스트 통과.
- **auth-server 3단계 Feature 4 완료 — 세션 관리(`/api/auth/refresh`·`/api/auth/logout`)**: Rotating refresh 와 logout 을 구현했다. **refresh**: Body 의 LINA refresh token 을 JWT 검증(서명·만료·`tokenType=refresh`) 후 **`users.refresh_token` 저장값과 대조** — stateless 검증만으론 회전 후 이전 토큰 재사용을 못 잡으므로 저장값 단일 대조가 재사용 거부의 근거(별도 토큰 테이블 미신설). 통과 시 groups/role 을 **DB 재조회**해 새 access JWT + 새 refresh 발급, `users.access_token`/`refresh_token` 덮어쓰기(이전 refresh 무효화). 만료/위조/재사용/로그아웃 상태/사용자 없음 전부 `401`. **logout**: Bearer 로 식별해 `users.refresh_token` 을 NULL 로 비움 → 이후 refresh `401`, 응답 `data: null`. **SecurityConfig**: login/callback/refresh `permitAll`(refresh 는 Body 토큰 자체 검증), logout 은 Bearer 필요 — 신규 `JwtAuthenticationFilter`(Bearer 검증→SecurityContext principal=userId) + 미인증 401 EntryPoint(공통 `ErrorResponse` 직렬화), **`/internal/**` 는 `denyAll`**(외부 차단 — Feature 5/7 에서 내부 호출자 인증으로 대체), 나머지 default deny. `User` 엔티티에 `rotateSessionTokens()`/`clearRefreshToken()` 추가(기존 시그니처 변경 없음). 검증: 실패 테스트 선작성(신규 14건 red 확인 — `SessionServiceTest` 7 + `AuthControllerTest` 추가 7: 회전·재사용 거부·logout 후 refresh 거부·Bearer 누락/위조 401·`/internal/**` 차단) 후 구현, `:auth-server:test` 78건 통과, `./scripts/verify.sh` 통과. 변경 파일: `token/SessionService.java`(신규), `config/JwtAuthenticationFilter.java`(신규), `oauth/dto/RefreshTokenRequest.java`(신규), `oauth/AuthController.java`(refresh/logout 추가), `config/SecurityConfig.java`, `token/entity/User.java`, `token/SessionServiceTest.java`(신규), `oauth/AuthControllerTest.java`, `backend/auth-server/current-plans.md`(Feature 4 `[x]`).
- **auth-server 3단계 Feature 3 완료 — OAuth Authorization Code Flow(`/api/auth/login`·`/api/auth/callback`)**: Confluence OAuth 로그인 진입점을 구현했다. **login**: Atlassian authorize 로 `302`(Wrapper 미적용), `state`(CSRF) 는 **in-memory 1회용+TTL 보관**(단일 인스턴스 전제 — 다중 인스턴스 시 외부 저장소 교체 필요, plan 에 명시)에 `mode`/`returnTo` 직렬화, `returnTo` 는 내부 경로만 허용(외부 URL/`//`/스킴 → `400`). **callback**: state 소비 → AUTH-02 code 교환 → AUTH-04 `accessible-resources`(cloudId) → user-info `/me`(accountId/email/name/picture — plan AUTH 목록 외 호출이나 accountId 취득에 필수) → **AUTH-05 `memberof` groups 페이징 전량 수집** → `users` upsert(신규 `role='USER'`) + `user_groups` 교체 적재 + Confluence 토큰·cloudId 암호화 저장(`user_tokens`, 기존 행은 `rotate`) → LINA access JWT + refresh 발급(`data: {accessToken, refreshToken, expiresAt(KST)}` — refresh 발급·`users.refresh_token` 저장은 응답 계약상 본 Feature 에 포함, 회전은 Feature 4). **정책 확정 3건**: ① groups 조회 실패 시 warn 로그 + **빈 `groups` 로 로그인 허용**(api-spec v2.6.0 'groups 빈 배열 허용' 정합), ② **admin 게이트는 groups 조회·모든 영속보다 먼저**(`mode=admin` + `role != ADMIN` → `403` "관리자 권한이 없는 계정입니다", 흔적 없음), ③ 멀티 사이트 = 단일이면 자동, 다중이면 `CONFLUENCE_SITE_URL` 일치만 허용(순서 기반 임의 선택 금지, 미설정/불일치 `500`, 0개 `401`). 실패 매핑: state 불일치/파라미터 누락 `400`, code 무효/Confluence 오류 `401`, 게이트 `403` — 전 실패 토큰 미발급, Confluence 토큰 응답 미포함(테스트 고정). 신규 `SecurityConfig` 는 Feature 3 시점 최소(login·callback permitAll + default deny)로 도입 후 Feature 4 에서 확장. 검증: 실패 테스트 선작성(신규 31건 red — WireMock AUTH-02/04/05·`/me`·페이징 5, Service 단위 13, MockMvc 8, state 5) 후 구현·전체 통과, `./scripts/verify.sh` 통과. **실제 Atlassian 미호출(전부 WireMock)** — 실 3LO 스모크·실 MySQL Flyway 검증은 Feature 8 게이트. 변경 파일: `oauth/{AuthController,OAuthLoginService,AtlassianOAuthClient,OAuthStateService,OAuthProperties}.java`(신규), `oauth/dto/` 5종(신규), `config/{SecurityConfig,RestClientConfig}.java`(신규), `token/entity/User.java`(`storeRefreshToken` 추가), `application.yml`(`api-base-uri`/`site-url`/`state-ttl-seconds`), 테스트 4종(신규), `backend/auth-server/current-plans.md`(Feature 3 `[x]`).

## 2026-06-11

- **`siteUrl` — §2-5 응답에 추가(신규 저장 없음, `admin_atlassian_credential.site_url` 전달)**: ingestion 이 Confluence `_links.webui` 상대경로를 absolute 출처 URL 로 정규화(→ Qdrant `webui_link`/RAG `sources[].url`)하도록, Data Ingestion Worker credential lookup(§2-5) 응답에 **`siteUrl`**(JSON, `https://{site}.atlassian.net`)을 추가했다. **새 컬럼/마이그레이션 없음** — 이미 `admin_atlassian_credential.site_url`(DB 컬럼, §6.4, V004)에 있는 값을 §2-5 에서 그대로 반환. **DB 컬럼=`site_url`(snake), JSON 필드=`siteUrl`(camel — 다른 응답 필드 accessToken/cloudId/expiresAt 과 통일).** BFF/FE 는 URL 조합 책임 없음. 콘텐츠 조회 REST 엔 미사용(그쪽은 cloudId 게이트웨이). §2-5 응답 = `{accessToken, cloudId, siteUrl, expiresAt}`. 변경 파일: `docs/api-spec.md`(§1-4/§2-2/§2-5/§3), `docs/db-schema.md`(§6.4), `backend/auth-server/current-plans.md`(AUTH-04·Feature 5·internal), `backend/bff-server/current-plans.md`(④). 검증: `./scripts/verify.sh` 통과.
- **auth-server current-plans 전체 정합성 점검 — V004(별도 테이블)·`refresh_token` 전파 누락 수정**: 하이브리드 모델·별도 테이블·`refresh_token` 도입 후, `current-plans.md` 의 완료 마커/변경 대상 목록이 아직 옛 상태(§6.1~§6.3, V001~V003, 엔티티 3개)를 가리키던 7곳을 바로잡았다: 신규 DB 목록·문서수정 필요·Done Definition 의 `§6.1~§6.3`→`§6.1~§6.4`, Feature 1 변경 대상/완료 마커에 `V004`·`admin_atlassian_credential` 엔티티·`users.refresh_token` 반영. 5개 파일(api-spec·db-schema·auth/bff plan·V001~V004) grep 교차검증: 옛 모델 잔재(`user_tokens.admin_api_token`) 0건, 하이브리드(admin-key=API Token/Basic/site URL/`admin_atlassian_credential`, 콘텐츠=OAuth/Bearer/gateway/`user_tokens`) 일관, §2-5 응답=OAuth `accessToken`+`cloudId`+`expiresAt`(refresh·401·502) ↔ Feature 5 일치, `refresh_token` 3곳 동일 확인. 검증: `./scripts/verify.sh` 통과. 변경 파일: `backend/auth-server/current-plans.md`, `docs/ai/working-log.md`.
- **`users.refresh_token` 컬럼 선반영(V001 직접 수정)**: LINA 세션 refresh token 저장 컬럼(`VARCHAR(512)`, NULL 허용)을 `V001__create_users.sql` 에 미리 추가했다. 발급/회전(rotating) 로직은 Feature 4 범위라 컬럼만 선반영. **V001 직접 수정**이므로 이미 `V001` 을 적용한 로컬 테스트 DB 는 Flyway 체크섬 불일치로 다음 기동이 실패 → drop&재마이그레이션 또는 `flyway repair` 필요(운영/공유 DB 없음). 대안(V005 ALTER)도 가능하나 "V001에 넣어달라" 요청대로 V001 편집. 변경 파일: `V001__create_users.sql`, `docs/db-schema.md`(§6.1 컬럼·변경이력), `backend/auth-server/current-plans.md`(Feature 3 주석), `docs/ai/working-log.md`.
- **admin-ingest 자격증명 = 하이브리드 모델 확정 + #6 확정(별도 테이블) — 문서 정합화**: Feature 0 게이트 결론을 명세 전반에 반영하되, 게이트의 "admin API Token" 적용 범위를 **admin-key 관리 호출에만** 한정하는 하이브리드로 최종 정정했다. **모델**: ① **admin-key 관리**(activate/deactivate) = admin API Token + **Basic auth** + **site URL**(`{siteUrl}/wiki/api/v2/admin-key`) — admin-key REST 는 OAuth2 앱 접근 불가(공식). ② **콘텐츠 조회**(pages/spaces/restrictions) = admin **OAuth accessToken** + **Bearer** + **게이트웨이**(`api.atlassian.com/ex/confluence/{cloudId}/...`) + Admin Key 헤더. (중간에 콘텐츠 조회까지 Basic+API Token 으로 적었던 것은 과잉정정 → 환원.) **#6 확정**: admin API Token 보관 = **별도 테이블 `admin_atlassian_credential`**(`site_url`+`admin_api_token_enc`, AES-GCM, timestamp 없음 — 정적이라 재발급 시 수동 UPDATE) — user_tokens 컬럼(중간안) 폐기, 콘텐츠 조회용 OAuth(`user_tokens`)와 자격증명·URL 체계가 달라 분리. siteUrl 도 이 테이블에 저장(admin-key 관리에 필요). §2-5 내부 API 는 **콘텐츠 조회용 OAuth accessToken + cloudId 반환**(refresh/expiresAt/401·502 유지), admin API Token 은 비반환(auth-server 내부 admin-key 관리 전용). 마이그레이션 **`V004__create_admin_atlassian_credential.sql`**(CREATE TABLE), `docs/db-schema.md` §6.4 신설·§6.2 user_tokens 환원·변경이력·§1 저장소표 갱신. changelog(과거 기록) 보존. 검증: `./scripts/verify.sh` 통과. 변경 파일: `docs/api-spec.md`(§1-4/§2-1/§2-2/§2-5/§3), `docs/db-schema.md`, `backend/bff-server/current-plans.md`, `backend/auth-server/current-plans.md`, `auth-server/.../db/migration/V004__create_admin_atlassian_credential.sql`(신규), `docs/ai/working-log.md`.

## 2026-06-10

- **auth-server 3단계 Feature 0 게이트 종료 — admin API Token(Basic auth) 모델 확정 + scope 확정**: 라이브 curl 없이 문서·실측으로 게이트를 닫았다. **판정 근거**: ① v2 `/admin-key` 공식 문서가 OAuth2/Forge/Connect 앱 접근 불가 명시 → 기존 "저장 OAuth token 으로 admin-key activate" 전제 성립 불가, ② Atlassian 팀 공식 답변 — `Atl-Confluence-With-Admin-Key` 헤더는 V1/V2 API 전반 동작(주체=admin 본인 credential), ③ 라이브 실측(2026-06-02 팀 테스트) — API Token Basic auth 로 activate `200` → restriction 페이지 404→200(목록 232→237개) → deactivate(`DELETE`) `204`, ④ 환경 전제 충족 — Premium plan·별도 admin·admin key 보유. **scope 확정**: 요약 scope 는 본문 미포함으로 부족 — `read:confluence-content.all`·`readonly:content.attachment:confluence`·`read:content.restriction:confluence`·`read:confluence-user`·`read:confluence-groups`·`read:confluence-space.summary`·`offline_access`. **수집측 핸드오프**: page-level restriction 비어도 상위 folder/space 계층으로 제한 가능(실측) → effective 권한 산출은 ingestion 측 해결 예정(ADR 0001). **plan 반영**: Feature 0 종결(검증 노트·§확정된 결정 행), Feature 1/5/6 에 API Token 모델 보강, §착수 전 결정 **#6 신규(open): API Token 보관 위치(MySQL 암호화 vs env Secret)**. 코드 변경 없음, `./scripts/verify.sh` 통과. 변경 파일: `backend/auth-server/current-plans.md`, `docs/ai/working-log.md`.
- **ACL fail-closed 정책 변경 — `userId` 만 fail-closed, `groups` 빈 배열 허용**: Confluence group 미소속 사용자(`memberof` 빈 결과)도 `userId` 로 user-level/공개 페이지가 매칭(가시성 = `allowed_users ∋ userId` **OR** `allowed_groups ∩ groups`)되므로, 기존 "`userId` 또는 `groups` 가 비면 차단"을 **`userId` 만 차단**으로 완화했다. 코드: `ChatService` ACL 게이트에서 `groups.isEmpty()` 제거(`userId` 만 게이트), `RagQueryCommand` `@NotEmpty` 제거(빈 배열 허용·import 정리), `ChatServiceTest` fail-closed 케이스를 `userId` 빈값 기준으로 변경 + "groups=[] 여도 `userId` 있으면 RAG 호출" 허용 테스트 신규. 문서: api-spec §2-1 fail-closed 노트·`groups` 필드(빈 배열 허용)·changelog, `backend/rules/rag-pipeline.md` §2, `backend/bff-server/current-plans.md` Feature 5 게이트. 검증: `./scripts/verify.sh`(format/lint/test 107건) 통과 + `:bff-server:bootRun` 스모크 — 기본(groups 있음)에서 검색·CRUD·이력·피드백·챗(ML 없어 `ML_CONNECTION_ERROR`) 전체 정상, `LINA_DEMO_GROUPS=""`(groups=[]) 재기동 시 챗이 `UNAUTHORIZED` 가 아닌 `ML_CONNECTION_ERROR`(게이트 통과)로 변경 적용 확인. **전제**: RAG 가 빈 `groups` + `userId` 로 user-level 매칭하는지 RAG 팀 확인 필요. 변경 파일: `chat/service/ChatService.java`, `rag/client/dto/RagQueryCommand.java`, `chat/service/ChatServiceTest.java`, `docs/api-spec.md`, `backend/rules/rag-pipeline.md`, `backend/bff-server/current-plans.md`.
- **api-spec `/ml/query` `userId`/`groups` 식별자 명세 정합화**: 3단계 확정(`userId`=Confluence accountId, `groups`=`groupId` 배열, Qdrant `allowed_groups` 정합)에 맞춰 §2-1 필드 정의·예시를 정정했다. 예시값을 2단계 데모(`user-001` / group name `Cloud-Control-Center`) → 실제 형식(accountId `712020:...` / groupId)으로 교체하고 "위 값은 3단계 실제값, 2단계 데모는 `lina.demo.*` 고정값" 노트 추가. §4-1 `users/me`·§4-2 `admin/users` 응답 예시의 `userId` 도 accountId 로 통일. changelog v2.6.0. 변경 파일: `docs/api-spec.md`.

- **auth-server 3단계 MySQL 스키마 확정(`users`/`user_groups`/`user_tokens`) + 전체 문서 정합화**: 3단계 인증 스키마를 설계해 Flyway 마이그레이션 `V001`~`V003` 으로 확정하고, 관련 문서를 새 모델로 일괄 정합화했다. **`users`**: PK `user_key`(BINARY16 UUID), `user_id`=Confluence accountId(UNIQUE — 문서/JWT/RAG `/ml/query`/Qdrant `allowed_users` 의 `userId` 와 동일 식별자, **이메일 아님**), `email`(UNIQUE, 로그인), `name`/`profile_image_url`/`role`(ENUM USER/ADMIN)/`access_token`(LINA 발급)/`last_login_at`/`created_at`/`updated_at`. **`user_groups`**(1:N): `(user_key, group_id)` 복합 PK + FK CASCADE, `group_id`=Confluence groupId(memberof `results[].id`) — 로그인 시 `memberof` 로 조회해 **영속**하며 앱이 `groups` 배열로 집계해 JWT/`/ml/query` 에 전달(DB 단수 `group_id` ↔ 와이어 복수 `groups`). **`user_tokens`**(1:1): `confluence_access/refresh_token_enc`(AES-GCM `VARBINARY(2048)`)+`cloud_id`+`access_token_expires_at` — **앱 내 Confluence 페이지 미리보기**(라이브 REST 호출) 용. 결정: (1) 미리보기를 "앱 내 렌더 + 외부 링크" 둘 다 지원 → access+refresh+cloudId 저장 필요(`user_tokens`), 챗(`/ml/query`)은 토큰 불필요. (2) groups 를 2026-06-09 의 "런타임 조회·테이블 없음" → **`user_groups` 테이블 영속**으로 전환. (3) `userId`=accountId 이므로 DB 컬럼명을 `atlassian_account_id`→`user_id`, 이메일 컬럼은 `email` 로 분리(이름 충돌 해소). (4) LINA refresh token 은 이번 단계 미고려(후속 — 멀티세션 필요 시 별도 테이블, 아니면 `users` 컬럼). 검증: 로컬 MySQL 8 도커(`lina-mysql`)에 `V001`~`V003` 적용해 `DESCRIBE`/FK/복합PK 중복차단/CASCADE/조회흐름(email→user_id(accountId)+groups) 확인. 변경 파일: `auth-server/.../db/migration/V001__create_users.sql`·`V002__create_user_groups.sql`·`V003__create_user_tokens.sql`(신규), `docs/db-schema.md`(§1 저장소 표·§6.1~§6.3 재작성·변경이력), `docs/architecture.md`(§6.1 groups→user_groups 영속), `backend/rules/domains.md`(§3), `backend/auth-server/current-plans.md`(Feature 1 마이그레이션/Entity/PK/groups 영속, Feature 3 callback `user_groups` 적재, 문서수정/변경대상 갱신).

## 2026-06-09

- **auth-server 3단계 Plan 재작성(bff-server 형식) + 착수 전 결정 5건 확정**: 기존 Feature A(11개 항목 묶음)를 **Feature 0~8**(변경 대상 파일 + 체크리스트 + 위험요소 + 완료 기준)로 분할 재작성하고, api-spec §3/§4-1/§2-5 와 정밀 대조했다. 확정: **#1** FE 단일 진입점=BFF, `/api/auth/**` 는 BFF gateway path-through 로 auth-server 핸들러 위임, `/api/users/me`·`/api/admin/*` 는 BFF 가 MySQL 직접 읽기. **#2** `groups` 취득=AUTH-05 `memberof` API, claim 값=**`groupId`**(`results[].id`, RAG `allowed_groups` 정합). **#3** 토큰 암호화=AES-GCM+env 키. **#4** JWT 서명=RS256(auth-server 개인키/BFF 공개키 검증). **#5** Admin Key 내부 API 는 3단계 인터페이스+WireMock, end-to-end 통합은 4단계. 외부 OAuth 계약에 AUTH-05(memberof) 추가, `cloudId` 멀티사이트 선택 주의 명시. 변경 파일: `backend/auth-server/current-plans.md`(전면 재작성).
- **`groupId` 확정·`user_space_acl` 미사용 전반 정합화**: JWT `groups`=`groupId`(Atlassian 공식 `memberof` 응답 `results[].id`) 확정에 따라 색인측 Qdrant `allowed_groups` 도 group **id** 적재로 정합(ADR §2 의 `name` 적재 지침 → `id`, §4 open question 해소). 스페이스 단위 권한 테이블 **`user_space_acl` 미사용**(페이지 ACL=수집단계 Qdrant payload) 을 전 문서에 반영. 변경 파일: `docs/db-schema.md`(§1·§6.2 placeholder), `docs/architecture.md`(§6.1), `docs/adr/0001-page-level-acl-source.md`(§2 색인 name→id·§4 #1 해소·Context user_space_acl 폐기), `backend/CLAUDE.md`(§2.2), `backend/rules/{auth,domains,rag-pipeline,testing}.md`(groups=groupId·memberof 출처·DataMongoTest/DataJpaTest 정합).

## 2026-06-08

- **bff-server 2단계 Feature 7 완료 — 대화 검색 API**: `GET /api/conversations/search?q=&page=&size=` 를 `ConversationSearchController`/`ConversationSearchService` 로 구현했다. 본인 활성 대화의 메시지 본문(`messages.content`)을 검색해 대화 단위로 묶고, 대화당 매칭 메시지 샘플 최대 3개 + `matchCount`(대화 내 총 매칭 수), `totalCount`(매칭 대화 총 수)를 반환한다. **권한 격리**는 `CurrentUserProvider` 의 현재 사용자(2단계 데모 `lina.demo.fixed-user-id`) 활성 대화 id 집합으로 검색 범위를 제한하는 방식 — 메시지에 `userId` 가 없으므로 `ConversationRepository.findByUserIdAndDeletedAtIsNull` 로 본인 활성 대화를 먼저 모으고 그 id 집합을 `MessageRepository.searchActiveByConversationIdsAndContent`(`@Query` `{conversationId:{$in},deletedAt:null,content:{$regex,$options:'i'}}`, sort `createdAt:1`)에 전달해 타 사용자 대화·삭제 대화·삭제 메시지를 모두 배제한다(soft delete 양쪽 필터). 검색어는 `SearchTextSupport.escapeRegex` 로 정규식 메타문자를 escape 한 뒤 `$regex`(case-insensitive)로 매칭하고, text index 는 후속(`docs/db-schema.md` §3.2). `q` 검증은 Service 에서 trim 후 길이 2~50자·`size` 1~50·`page` ≥0 위반 시 `400 INVALID_SEARCH_QUERY`(Controller `q` defaultValue `""` 로 누락도 동일 코드 처리). 응답 하이라이트는 `SearchTextSupport.extract` 가 첫 매칭 좌우 ~40자 발췌(잘림 시 `...` prefix/suffix)와 `snippet` 기준 `matchPositions: [[start,end]]`(UTF-16, end exclusive)만 생성하고 **HTML 은 만들지 않는다**(XSS 안전성). 정렬은 `lastMessageAt` DESC(PoC, 관련도 점수 미적용), 모든 timestamp 는 KST(`Asia/Seoul`) 직렬화(확정된 결정 #6). `common` `ErrorCode` 에 `INVALID_SEARCH_QUERY`(400) 추가 — 도메인 특화 코드 최초 사례(`docs/api-spec.md` Common 노트). 응답이 `docs/api-spec.md` §1-2·`db-schema.md` §3.2 와 일치해 문서 변경은 불필요. 검증: 실패 테스트 선작성(서비스/유틸 스텁이 `UnsupportedOperationException` → 23개 중 15개 red 확인) 후 구현, `SearchTextSupportTest`(escape·snippet, 7) + `MessageContentSearchRepositoryTest`(@DataMongoTest: id집합 매칭/집합 밖 미노출/soft delete 미노출/case-insensitive/메타문자 escape, 5) + `ConversationSearchServiceTest`(검증·그룹핑·matchCount cap3·정렬·페이지네이션·escape 위임·KST, 8) + `ConversationSearchControllerTest`(정상 200·400 INVALID_SEARCH_QUERY·page 형식 400 INVALID_REQUEST, 3) 전부 통과, `./scripts/verify.sh`(format/lint/test) 통과. 변경 파일: `chat/controller/ConversationSearchController.java`(신규), `chat/service/ConversationSearchService.java`(신규), `chat/support/SearchTextSupport.java`(신규), `chat/dto/ConversationSearchResponse.java`·`ConversationSearchResultResponse.java`·`MatchedMessageResponse.java`(신규), `chat/repository/ConversationRepository.java`(`findByUserIdAndDeletedAtIsNull` 추가)·`MessageRepository.java`(`searchActiveByConversationIdsAndContent` 추가), `common/exception/ErrorCode.java`(`INVALID_SEARCH_QUERY` 추가), 위 4개 테스트(신규), `backend/bff-server/current-plans.md`(Feature 7 체크리스트 `[x]`·완료 노트).

- **bff-server 2단계 Feature 6 완료 — 피드백 API**: `POST /api/messages/{messageId}/feedback` 를 `FeedbackController`/`FeedbackService` 로 구현했다. `messageId` 기준 upsert — 기존 피드백이 없으면 생성(`201` "피드백 등록 성공"), 있으면 갱신(`200` "피드백 수정 성공")하며, 신규/갱신 여부는 Service 가 반환하는 nested record `FeedbackResult(boolean created, FeedbackResponse response)` 로 전달해 Controller 가 HTTP status 를 결정한다. 갱신 시 `Feedback.update(rating, comment)` 로 `feedbackId` 와 최초 `createdAt` 을 유지(메시지당 1건 `uniq_feedbacks_message` 정합). 요청 DTO `CreateFeedbackRequest` 는 `rating`(`@NotNull`, `LIKE`/`DISLIKE` UPPER) 필수·`comment` 선택이고, 누락/잘못된 rating 은 공통 핸들러에서 `400 INVALID_REQUEST` 로 처리된다. 대상 메시지 존재 검증은 `MessageRepository.existsByMessageIdAndDeletedAtIsNull` 를 추가해 soft delete 규칙(`deletedAt == null`)을 지키며, 없는/삭제 메시지는 `404 RESOURCE_NOT_FOUND`. 응답 DTO `FeedbackResponse`(`feedbackId`/`messageId`/`rating`/`createdAt`)는 `createdAt` 을 KST(`Asia/Seoul`) `ZonedDateTime` 으로 직렬화(확정된 결정 #6). 응답 구조가 `docs/api-spec.md` §1-3 과 일치해 명세 변경은 불필요. 검증: 실패 테스트 선작성(컴파일 실패로 red 확인) 후 `FeedbackServiceTest`(신규/갱신/404/DTO 타입) 4건 + `FeedbackControllerTest`(201/200/400 누락/400 잘못된 값/404) 5건 통과, `./scripts/verify.sh` 통과. 변경 파일: `feedback/controller/FeedbackController.java`(신규), `feedback/service/FeedbackService.java`(신규), `feedback/dto/CreateFeedbackRequest.java`·`FeedbackResponse.java`(신규), `feedback/service/FeedbackServiceTest.java`·`feedback/controller/FeedbackControllerTest.java`(신규), `feedback/entity/Feedback.java`(`update()` 보강), `chat/repository/MessageRepository.java`(`existsByMessageIdAndDeletedAtIsNull` 추가), `backend/bff-server/current-plans.md`(Feature 6 체크리스트 `[x]`·완료 노트).

## 2026-06-07

- **bff-server 2단계 Feature 5 열두 번째 항목 완료 — ChatService unit·Controller SSE 시퀀스 검증**: 기존 `ChatServiceTest` 가 `RagClient` 와 `ChatMessagePersistenceService` mock 으로 RAG command 조립, ACL fail-closed, 정본 이벤트 필터/가공, `done` assistant 저장, `error` passthrough, `RagClientException` 의 SSE `error` 종료 경로를 검증하고 있음을 확인했다. `ChatControllerTest` 에 정상 SSE 시퀀스 검증을 추가해 `status` → `token` → `done` 순서와 payload 가 Wrapper 없이 `text/event-stream` 응답에 직렬화되는 경로를 고정했다. 검증: `cd backend && ./gradlew :bff-server:test --tests com.lina.bff.chat.service.ChatServiceTest --tests com.lina.bff.chat.controller.ChatControllerTest` 통과, `cd backend && ./gradlew :bff-server:spotlessJavaCheck` 통과, `cd backend && ./gradlew :bff-server:test` 전체 테스트 통과.
- **bff-server 2단계 Feature 5 열한 번째 항목 완료 — RAG WireMock·SSE error 종료 테스트 고정**: `RagClientTest` 의 WireMock 경계가 `/ml/query` 정상 SSE 스트림 파싱, ML 5xx 의 `ML_SERVER_ERROR` 변환, read timeout 의 `ML_TIMEOUT` 변환, 요청 body 의 `accessToken`/`cloudId` 미포함을 고정한다. `ChatServiceTest` 에 MockMvc 기반 `streamChat` 실패 경로를 추가해 `RagClientException` 이 FE SSE `error` 이벤트로 전송되고 연결이 완료되며 assistant 메시지는 저장하지 않음을 검증한다. 현재 BFF 에는 `/api/admin/ingest` 구현이나 RabbitMQ payload 코드가 없으므로, checklist 의 `/ml/ingest`/job payload credential 미포함 테스트는 4단계 구현 시 `jobId`/`adminUserId`/`mode` 중심 payload 로 추가한다. 검증: `cd backend && ./gradlew :bff-server:test --tests com.lina.bff.rag.client.RagClientTest --tests com.lina.bff.chat.service.ChatServiceTest --tests com.lina.bff.chat.controller.ChatControllerTest` 통과, `cd backend && ./gradlew :bff-server:spotlessJavaCheck` 통과.
- **bff-server 2단계 Feature 5 열 번째 항목 완료 — PoC credential 노출 표면 제거**: `/api/admin/ingest` 구현은 아직 4단계 범위라 실제 Data Ingestion 호출/RabbitMQ payload 코드는 없지만, 현재 BFF 코드 표면에서 Confluence credential 을 보관·전달할 여지를 제거했다. `application.yml` 의 stale 한 `lina.confluence.access-token` / `cloud-id` 설정 placeholder 를 삭제하고, Data Ingestion 주석을 "BFF 는 credential set 을 조회하거나 HTTP/RabbitMQ payload 에 싣지 않으며 Data Ingestion Worker 가 auth-server 내부 API 로 조회"로 정정했다. `SensitiveConfigurationTest` 를 추가해 actuator 노출 endpoint 에 `env`/`heapdump` 가 없고 BFF 설정에 `lina.confluence.*` credential key 가 없음을 고정했다. RAG `/ml/query` credential 미포함 검증은 기존 `RagClientTest` 로 유지된다. 검증: `cd backend && ./gradlew :bff-server:test --tests com.lina.bff.config.SensitiveConfigurationTest --tests com.lina.bff.rag.client.RagClientTest --tests com.lina.bff.config.RagClientConfigTest` 통과, `cd backend && ./gradlew :bff-server:spotlessJavaCheck` 통과.
- **bff-server 2단계 Feature 5 아홉 번째 항목 완료 — ML 실패/idle timeout SSE `error` 종료**: `RagClient` 가 `/ml/query` 5xx 를 `ML_SERVER_ERROR`, 연결 실패·스트림 중단을 `ML_CONNECTION_ERROR`, read timeout 을 `ML_TIMEOUT` 으로 구분해 `RagClientException.errorCode` 에 싣도록 했다. `lina.rag.sse-timeout-ms` 는 RAG RestClient read timeout 과 `SseEmitter` timeout 에 적용되며, `ChatService.streamChat()` 은 `RagClientException` 또는 emitter timeout 을 `error` SSE 이벤트로 전송한 뒤 연결을 정리한다. `ChatController` 는 SSE 응답 헤더 `text/event-stream`, `Cache-Control: no-cache`, `Connection: keep-alive`, `X-Accel-Buffering: no` 를 명시한다. 장시간 phase 의 keep-alive 는 RAG 가 보내는 `status` 이벤트를 BFF 가 그대로 중계해 idle timeout 이 갱신되는 방식으로 처리한다. 검증: `cd backend && ./gradlew :bff-server:test --tests com.lina.bff.rag.client.RagClientTest --tests com.lina.bff.config.RagClientConfigTest --tests com.lina.bff.chat.controller.ChatControllerTest --tests com.lina.bff.chat.service.ChatServiceTest` 통과, `cd backend && ./gradlew :bff-server:spotlessJavaCheck` 통과.
- **bff-server 2단계 Feature 5 여덟 번째 항목 완료 — `meta.title` 기반 대화 제목 1회 자동 설정**: RAG SSE `meta` 이벤트의 `title` 값을 `ChatService.ChatStreamState` 에 누적하고, `done` 수신 시 assistant 메시지를 저장하는 경계에서 `ChatMessagePersistenceService.saveAssistantMessage(...)` 로 전달하도록 했다. `Conversation.applyGeneratedTitleIfDefault(...)` 를 추가해 현재 제목이 기본값 `"새 대화"` 일 때만 공백 trim 후 제목을 갱신하고, 이미 자동/수동으로 바뀐 제목은 `meta.title` 을 무시한다. 빈 title 도 무시한다. `"새 대화"` 는 null 제목을 피하기 위한 기본 표시값이면서 자동 제목 미설정 상태를 나타내는 sentinel 로 사용한다. 단, 사용자가 제목을 직접 `"새 대화"` 로 바꾸는 경우까지 엄격히 구분하려면 후속에 `titleSource` / `titleAutoGenerated` 같은 별도 필드가 필요하다. 검증: `cd backend && ./gradlew :bff-server:test --tests com.lina.bff.chat.service.ChatServiceTest --tests com.lina.bff.chat.service.ChatMessagePersistenceServiceTest --tests com.lina.bff.chat.controller.ChatControllerTest` 통과, `cd backend && ./gradlew :bff-server:spotlessJavaCheck` 통과.
- **bff-server 2단계 Feature 5 리팩토링 — SSE 중계와 메시지 저장 책임 분리**: `ChatService` 에 있던 대화 조회, history 변환, user/assistant 메시지 저장, `lastMessageAt` 갱신 로직을 `ChatMessagePersistenceService` 로 분리했다. `ChatService` 는 이제 RAG 요청 조립, SSE 이벤트 필터/누적/FE 중계, `done`/`error` payload 가공에 집중하고, 저장은 persistence service 의 `loadConversation` / `recentHistory` / `saveUserMessage` / `saveAssistantMessage` 로 위임한다. 현재 로컬/PoC Mongo 는 standalone 전제이므로 `MongoTransactionManager` 와 `@Transactional` 은 적용하지 않는다. Mongo 를 replica set/sharded cluster 로 전환하면 긴 SSE 메서드가 아니라 persistence service 의 짧은 write 메서드에만 transaction 적용을 검토한다. 검증: `cd backend && ./gradlew :bff-server:test --tests com.lina.bff.chat.service.ChatServiceTest --tests com.lina.bff.chat.service.ChatMessagePersistenceServiceTest --tests com.lina.bff.chat.controller.ChatControllerTest` 통과, `cd backend && ./gradlew :bff-server:spotlessJavaCheck` 통과.
- **bff-server 2단계 Feature 5 여섯·일곱 번째 항목 완료 — SSE boundary 가공과 메시지 영속**: `ChatService.relayRagQuery()` 가 활성 대화의 기존 history 를 먼저 읽은 뒤 user 메시지를 선저장하고, RAG 스트림의 `token`/`sources`/`verification` 을 누적해 `done` 수신 시 assistant 메시지를 MongoDB `messages` 컬렉션에 저장하도록 했다. 저장된 assistant 의 `messageId` 를 `done` payload 에 채워 FE 로 중계하고, `done.timestamp` 는 assistant 생성 시각 기준 KST 로 보강/직렬화한다. `error` 이벤트는 유효한 `errorCode`/`message` 를 그대로 중계하며 assistant 메시지는 저장하지 않는다. user/assistant 저장 시 `Conversation.recordMessageAt()` 으로 `lastMessageAt`/`updatedAt` 을 갱신해 대화 목록 정렬 키도 최신 메시지 시각을 반영한다. 이번 loop 는 boundary 가공과 메시지 영속 항목까지 완료했고, `meta.title` 자동 제목 설정과 ML timeout/error 연결 정리는 후속 체크리스트로 남겼다. 검증: `cd backend && ./gradlew :bff-server:test --tests com.lina.bff.chat.service.ChatServiceTest --tests com.lina.bff.chat.controller.ChatControllerTest` 통과, `cd backend && ./gradlew :bff-server:spotlessJavaCheck` 통과.
- **bff-server 2단계 Feature 5 다섯 번째 항목 완료 — SSE 7종 정본 중계와 KST timestamp 가공**: `ChatService` 에 RAG SSE 이벤트 중계 필터/가공 레이어를 추가해 `docs/api-spec.md` §1-1 의 정본 7종(`status`/`token`/`sources`/`verification`/`meta`/`done`/`error`)만 FE 로 전달하도록 했다. `done` 또는 `error` 수신 시 스트림을 terminal 로 보고 `SseEmitter` 를 종료하며, terminal 이후 도착한 추가 이벤트와 정의되지 않은 이벤트는 무시한다. `sources[].sourceUpdatedAt` 은 KST(`+09:00`) 문자열로 변환하고, `done.timestamp` 는 ML payload 에 있으면 KST 로 변환, 없으면 현재 시각 KST 로 보강한다. 이번 loop 는 이벤트 집합 중계/종료/KST 직렬화 항목에 한정했고, `done` 의 assistant `messageId` 저장 보강, user/assistant 메시지 영속, `meta.title` 자동 제목 설정, ML timeout/error 처리는 후속 체크리스트로 남겼다. 검증: sandbox `~/.gradle` lock 접근 실패로 권한 상승해 `cd backend && ./gradlew :bff-server:test --tests com.lina.bff.chat.service.ChatServiceTest --tests com.lina.bff.chat.controller.ChatControllerTest` 통과, `cd backend && ./gradlew :bff-server:spotlessJavaCheck` 통과.
- **bff-server 2단계 Feature 5 실행기 개선 — Spring 관리 Virtual Thread TaskExecutor 적용**: `ChatService.streamChat()` 에서 직접 `Thread.ofVirtual().start(...)` 를 호출하던 방식을 `chatSseTaskExecutor` 주입으로 변경했다. 새 `ChatSseExecutorConfig` 는 `SimpleAsyncTaskExecutor("chat-sse-")` 에 `setVirtualThreads(true)` 를 설정해 요청별 SSE 중계 작업을 Spring Bean lifecycle 안에서 실행한다. 네트워크 연결 구조는 여전히 `FE-BFF` SSE 연결과 `BFF-RAG` SSE 응답 연결 2개이며, executor 변경은 연결 수를 줄이는 것이 아니라 스레드 생성/관리 방식을 Spring 관리형으로 정리하는 개선이다. 검증: `cd backend && ./gradlew :bff-server:test --tests com.lina.bff.chat.service.ChatServiceTest --tests com.lina.bff.chat.controller.ChatControllerTest --tests com.lina.bff.rag.client.RagClientTest` 통과, `cd backend && ./gradlew :bff-server:spotlessCheck` 통과.
- **bff-server 2단계 Feature 5 네 번째 항목 완료 — 챗 SSE 엔드포인트 `SseEmitter` 반환**: `ChatController` 와 `ChatRequest` 를 추가해 `POST /api/conversations/{conversationId}/chat` 요청을 받도록 했다. 컨트롤러는 `produces = text/event-stream` 으로 `SseEmitter` 를 직접 반환하며 공통 `ApiResponse` Wrapper 를 적용하지 않는다. `ChatService.streamChat()` 는 `SseEmitter` 를 생성해 Virtual Thread 에서 기존 `relayRagQuery()` 를 실행하고, 현재 단계에서는 파싱된 RAG SSE 이벤트를 같은 event 이름/data 로 emitter 에 전송한다. 이번 loop 는 Feature 5의 `SseEmitter` 반환/Wrapper 미적용 체크리스트 하나에 한정했고, `done` messageId 가공, assistant/user 메시지 영속, idle timeout/error 세부 처리는 후속 체크리스트로 남겼다. 검증: 실패 테스트 선작성 후 sandbox `~/.gradle` lock 접근 실패로 권한 상승해 `cd backend && ./gradlew :bff-server:test --tests com.lina.bff.chat.controller.ChatControllerTest --tests com.lina.bff.chat.service.ChatServiceTest --tests com.lina.bff.rag.client.RagClientTest` 통과, `cd backend && ./gradlew :bff-server:spotlessCheck` 통과.
- **bff-server 2단계 Feature 5 세 번째 항목 완료 — ACL fail-closed SSE error 종료**: `ChatService.relayRagQuery()` 의 ACL 게이트를 보강해 `CurrentUserProvider` 의 `userId` 가 비어 있거나 `groups` 가 빈 배열이면 `/ml/query` 를 호출하지 않고 내부 SSE 이벤트 `error` payload `{ "errorCode": "UNAUTHORIZED", "message": "RAG 호출에 필요한 ACL 정보가 없습니다." }` 를 발행한 뒤 종료하도록 했다. 기존 활성 대화 확인은 유지하며, ACL 누락 경로에서 `RagClient.streamQuery()` 가 호출되지 않음을 테스트로 고정했다. 이번 loop 는 Feature 5의 ACL fail-closed 체크리스트 하나에 한정했고, 외부 `SseEmitter` Controller 중계와 user/assistant 메시지 영속은 후속 loop 로 남겼다. 검증: 실패 테스트 선작성 후 sandbox `~/.gradle` lock 접근 실패로 권한 상승해 `cd backend && ./gradlew :bff-server:test --tests com.lina.bff.chat.service.ChatServiceTest --tests com.lina.bff.rag.client.RagClientTest` 통과.
- **bff-server 2단계 Feature 5 두 번째 항목 완료 — `/ml/query` 요청 payload 정합성 검증**: `ChatService` 가 기존 활성 대화 확인 후 `CurrentUserProvider` 의 데모 ACL(`userId`/`groups`)과 `MessageRepository` 의 시간순 메시지 이력 중 최근 `lina.rag.history-turns` 개를 조합해 `RagQueryCommand` 로 전달하는 경로를 확인했다. 요청 본문은 `question`, `conversationId`, `history[].role` lowercase 저장값(`user`/`assistant`), `userId`, `groups`, `stream: true` 만 포함하고, LINA API 표면에서 제거된 `spaceKey` 와 Confluence credential 인 `accessToken`/`cloudId` 를 포함하지 않는다. 이번 loop 는 Feature 5의 두 번째 체크리스트 상태 반영에 한정했고, 다음 항목인 ACL fail-closed SSE `error` 종료/외부 `SseEmitter` 중계/영속 처리는 후속 loop 로 남겼다. 검증: sandbox 에서 `~/.gradle` lock 접근이 막혀 권한 상승 후 `cd backend && ./gradlew :bff-server:test --tests com.lina.bff.chat.service.ChatServiceTest --tests com.lina.bff.rag.client.RagClientTest` 통과.

## 2026-06-05

- **bff-server 2단계 Feature 5 첫 항목 완료 — RAG Client 동기 SSE 파싱 경계**: `rag/client/RagClient` 와 `RagQueryCommand`/`RagSseEvent` DTO 를 추가해 `/ml/query` 호출을 Service 계층 밖 `rag/client/` 패키지로 격리했다. 구현은 Spring MVC/Virtual Threads 규칙에 맞춰 동기 `RestClient.exchange()` + `InputStream`/`BufferedReader` 로 `event:`/`data:` SSE 쌍을 순서대로 파싱하며, `Mono`/`Flux`/WebFlux 타입은 사용하지 않았다. 요청 DTO 는 camelCase 계약(`question`, `userId`, `groups`, `conversationId`, `history`, `stream`)을 따르고 `accessToken`/`cloudId` 를 포함하지 않는다. 이번 loop 범위는 Feature 5 첫 체크리스트 항목까지이며, ACL fail-closed, ChatService 영속, SseEmitter 외부 중계, done/error boundary 가공은 다음 체크리스트로 남겼다. 검증: 실패 테스트 선작성 후 `../gradlew :bff-server:test --tests com.lina.bff.rag.client.RagClientTest` 통과. 참고: sandbox 에서 `~/.gradle` lock 접근이 막혀 Gradle 명령은 권한 상승으로 실행했다.
- **Admin Key 말소 흐름 RabbitMQ completion event 방식으로 갱신**: 2026-06-04 에 정리했던 BFF Virtual Thread watcher + `/ml/ingest/status/{jobId}` polling 방식은 이번 결정으로 대체. `/api/admin/ingest` 는 RabbitMQ 기반 비동기 수집 플로우로 문서화했다. BFF 는 관리자 수집 요청에서 auth-server 내부 API 로 Admin Key 를 activate 한 뒤 RabbitMQ ingest job 을 직접 발행하거나 Data Ingestion Pipeline 의 `/ml/ingest` 에 job 발행을 위임한다. Data Ingestion Worker 는 job consume 후 `adminUserId` 로 auth-server 내부 credential 조회 API 를 호출해 admin OAuth `accessToken` + `cloudId` 를 함께 조회하고, Confluence 호출 시 `Authorization: Bearer {admin accessToken}` + `Atl-Confluence-With-Admin-Key: true` 헤더를 사용한다. RabbitMQ job/completion payload 에는 `jobId`, `adminUserId`, `mode`, `status` 등 작업 식별/상태 정보만 포함하고 `accessToken`/`refreshToken`/`cloudId` 는 포함하지 않는 보안 원칙을 명시했다. 작업 완료/실패 시 Data Ingestion Pipeline 이 completion event 를 발행하고 BFF consumer 가 이를 consume해 auth-server `POST /internal/admin/key/deactivate` 를 호출한다. deactivate 대상은 OAuth token 이 아니라 Atlassian Admin Key 활성 상태이며, `jobId` 기준 idempotent 로 취급한다. 장애 정책 초안도 추가: BFF 재시작/consumer 장애 시 durable queue event 재처리, Admin Key 60분 TTL fallback, deactivate 최대 5회 재시도 후 DLQ 이동, DLQ 는 원인 조치 뒤 event 재발행 또는 auth-server deactivate 수동 호출로 복구. 변경 파일: `docs/api-spec.md` v2.5.0·§1-4·§2-2·§3, `backend/bff-server/current-plans.md` 4단계 placeholder·RabbitMQ completion event 체크리스트, `backend/auth-server/current-plans.md` 내부 credential 조회 API·Admin Key activate/deactivate 관계, `docs/adr/0001-page-level-acl-source.md` §2.1 역할 분리와 MQ payload 원칙.
- **bff-server 2단계 Feature 4 완료 — 메시지 이력 조회 API**: `GET /api/conversations/{conversationId}/messages` 를 `ConversationController`/`ConversationService` 에 추가했다. 활성 대화 존재를 `findByConversationIdAndDeletedAtIsNull` 로 확인하고, 메시지는 기존 Repository 쿼리 `findByConversationIdAndDeletedAtIsNullOrderByCreatedAtAsc` 로 `deletedAt == null` 필터와 `createdAt ASC` 순서를 보장한다. 응답은 `MessageHistoryResponse`/`MessageResponse`/`SourceResponse` DTO 로 변환해 Entity 직접 반환을 피했고, `role` lowercase(`user`/`assistant`), sources, `confidenceScore`, `verificationResult` 를 포함한다. `createdAt` 과 `sources[].sourceUpdatedAt` 은 KST(`Asia/Seoul`) `ZonedDateTime` 으로 변환한다. 검증: 실패 테스트 선작성 후 `./gradlew :bff-server:test --tests com.lina.bff.chat.service.ConversationServiceTest --tests com.lina.bff.chat.controller.ConversationControllerTest` 통과, `./gradlew :bff-server:test` 통과, `./gradlew :bff-server:spotlessApply` 통과, `./gradlew :bff-server:check` 통과. 참고: `./scripts/format.sh` 는 종료코드 0이었지만 sandbox 에서 `~/.gradle` lock 접근 실패 후 root `spotlessApply` 미탐지 메시지를 출력해, 실제 포맷은 `:bff-server:spotlessApply` 로 수행했다.

## 2026-06-04

- **bff-server 2단계 Feature 3 아홉 번째 항목 완료 — Service/Controller 테스트 종합 검증**: 대화 CRUD API 의 Service Unit Test(Repository Mock) 와 Controller MockMvc 테스트가 정상/검증실패/404/Wrapper/KST 범위를 모두 고정하도록 보강했다. Service 에 `listConversations` 현재 사용자 식별자 누락 시 `INVALID_REQUEST` 및 Repository 미호출 검증을 추가했고, Controller 에 `GET /api/conversations` Service 검증 실패가 공통 `ErrorResponse(400)` 로 반환되는 케이스를 추가했다. 기존 테스트로 POST/GET/PATCH/DELETE 정상 Wrapper, PATCH/DELETE 404, 요청 본문·파라미터 형식 400, KST `+09:00` 직렬화를 함께 검증한다. 검증: `./gradlew :bff-server:test --tests 'com.lina.bff.chat.service.ConversationServiceTest' --tests 'com.lina.bff.chat.controller.ConversationControllerTest'` 통과, `./scripts/format.sh` 통과, `./scripts/lint.sh` 통과.
- **bff-server 2단계 Feature 3 여덟 번째 항목 완료 — 대화 CRUD 응답 timestamp KST `ZonedDateTime` 보장**: 대화 CRUD 응답 DTO 의 timestamp 필드가 `Instant`/문자열이 아니라 `ZonedDateTime` 타입으로 노출되는 계약을 테스트로 고정했다(`CreateConversationResponse.createdAt`, `ConversationSummaryResponse.lastMessageAt`, `UpdateConversationResponse.updatedAt`). Service 변환은 기존 `toKst(...).atZone(Asia/Seoul)` 경로를 유지했고, 목록 응답의 `lastMessageAt` 은 KST zone 과 원본 instant 보존을 함께 검증했다. Controller MockMvc 의 `+09:00` 직렬화 검증도 함께 재실행했다. 검증: `./gradlew :bff-server:test --tests 'com.lina.bff.chat.service.ConversationServiceTest' --tests 'com.lina.bff.chat.controller.ConversationControllerTest'` 통과, `./scripts/format.sh` 통과, `./scripts/lint.sh` 통과.
- **bff-server 2단계 Feature 3 일곱 번째 항목 완료 — 필수 필드 누락/형식 오류 400 ErrorResponse 보장**: `PATCH /api/conversations/{id}` 의 요청 본문 누락·잘못된 JSON 형식, `GET /api/conversations` 의 `page` 타입 오류가 공통 `ErrorResponse`(`isSuccess=false`, `code=400`, `errorCode=INVALID_REQUEST`) 로 반환되도록 테스트를 추가했다. Spring MVC 입력 변환 단계에서 발생하는 `HttpMessageNotReadableException`, `MethodArgumentTypeMismatchException` 을 `GlobalExceptionHandler` 에서 `INVALID_REQUEST` 로 매핑해 fallback 500 으로 새지 않게 했다. 검증: 실패 테스트 선작성 후 `./gradlew :bff-server:test --tests 'com.lina.bff.chat.controller.ConversationControllerTest'` 통과, `./scripts/format.sh` 통과, `./scripts/lint.sh` 통과.
- **bff-server 2단계 Feature 3 여섯 번째 항목 완료 — 존재하지 않거나 삭제된 대화 404 보장**: `PATCH`/`DELETE` 의 대화 단건 접근이 `findByConversationIdAndDeletedAtIsNull` 를 통해 활성 대화만 대상으로 하며, 빈 결과는 `BizException(RESOURCE_NOT_FOUND)` 로 변환되는 경로를 테스트로 고정했다. Service 테스트에 수정/삭제 각각의 missing-or-deleted 케이스를 추가했고, Controller 테스트에 `PATCH /api/conversations/{id}` 404 ErrorResponse 검증을 보강했다(DELETE 404 검증은 기존 테스트 유지). 검증: `./gradlew :bff-server:test --tests 'com.lina.bff.chat.service.ConversationServiceTest' --tests 'com.lina.bff.chat.controller.ConversationControllerTest'` 통과, `./scripts/format.sh` 통과, `./scripts/lint.sh` 통과.
- **bff-server 2단계 Feature 3 다섯 번째 항목 완료 — `Conversation.isPinned`/Repository 정렬/인덱스 보강 확인**: 앞선 POST/GET 구현에서 반영된 `Conversation.isPinned` 기본값 false, 복합 인덱스 `{userId, deletedAt, isPinned:-1, lastMessageAt:-1}`, Repository 메서드 `findByUserIdAndDeletedAtIsNullOrderByIsPinnedDescLastMessageAtDesc` 를 Feature 3 체크리스트 항목으로 검증 완료. Repository Javadoc 의 stale 한 인덱스/정렬 설명을 실제 구현과 맞췄고, `ConversationRepositoryTest` 에 `isPinned=false` 기본값 검증을 추가했다. 검증: `./gradlew :bff-server:test --tests 'com.lina.bff.chat.repository.ConversationRepositoryTest'` 통과, `./scripts/format.sh` 통과, `./scripts/lint.sh` 통과.
- **bff-server 2단계 Feature 3 네 번째 항목 완료 — `DELETE /api/conversations/{conversationId}`**: 대화 삭제 API를 soft delete 로 구현. `ConversationService.deleteConversation()` 이 활성 대화를 조회한 뒤 `Conversation.markDeleted()` 로 `deletedAt` 을 채우고 동일 문서를 저장한다. Controller 는 `ApiResponse<Void>` 로 `200`, message `"대화 삭제 성공"`, `data: null` 을 반환한다. 없는/삭제된 대화는 기존 `RESOURCE_NOT_FOUND` 경로를 사용한다. 검증: 실패 테스트 선작성 후 `./gradlew :bff-server:test --tests 'com.lina.bff.chat.service.ConversationServiceTest' --tests 'com.lina.bff.chat.controller.ConversationControllerTest'` 통과, `./scripts/format.sh` 통과, `./scripts/lint.sh` 통과.
- **bff-server 2단계 Feature 3 세 번째 항목 완료 — `PATCH /api/conversations/{conversationId}`**: 대화 제목/고정 여부 부분 수정 API 구현. `UpdateConversationRequest`/`UpdateConversationResponse` DTO를 추가했고, `Conversation.update(title, isPinned)` 로 전달된 필드만 수정하면서 `updatedAt` 을 갱신한다. `title` 과 `isPinned` 이 모두 없으면 Service 에서 `BizException(INVALID_REQUEST)` 로 차단한다. 응답 `updatedAt` 은 KST(`Asia/Seoul`) `ZonedDateTime` 으로 변환한다. 테스트 샘플 제목은 API 예시의 S3 문구 대신 기능 의미에 집중하도록 `"수정된 대화 제목"` 으로 변경했다. 검증: 실패 테스트 선작성 후 `./gradlew :bff-server:test --tests 'com.lina.bff.chat.service.ConversationServiceTest' --tests 'com.lina.bff.chat.controller.ConversationControllerTest'` 통과, `./scripts/format.sh` 통과, `./scripts/lint.sh` 통과.
- **bff-server 2단계 Feature 3 두 번째 항목 완료 — `GET /api/conversations`**: 대화 목록 조회 API 구현. `CurrentUserProvider` 의 고정 데모 사용자 기준으로 `deletedAt == null` 활성 대화만 조회하고, `isPinned DESC` → `lastMessageAt DESC` 순서로 페이징한다. 응답 DTO(`ConversationListResponse`, `ConversationSummaryResponse`)를 추가해 Entity 직접 반환을 피했고 `lastMessageAt` 은 KST(`Asia/Seoul`) `ZonedDateTime` 으로 변환한다. Repository 메서드는 `findByUserIdAndDeletedAtIsNullOrderByIsPinnedDescLastMessageAtDesc` 로 변경했다. 검증: 실패 테스트 선작성 후 `./gradlew :bff-server:test --tests 'com.lina.bff.chat.service.ConversationServiceTest' --tests 'com.lina.bff.chat.controller.ConversationControllerTest' --tests 'com.lina.bff.chat.repository.ConversationRepositoryTest'` 통과, `./scripts/format.sh` 통과, `./scripts/lint.sh` 통과. 참고: sandbox 에서 Gradle Wrapper 의 `~/.gradle` lock 접근이 막혀 Gradle 기반 명령은 권한 상승으로 실행했다.
- **bff-server 2단계 Feature 3 첫 항목 완료 — `POST /api/conversations`**: 새 대화 생성 API 구현. 현재 사용자(`CurrentUserProvider`) 기준으로 기본 제목 `"새 대화"`, `isPinned=false`, UTC 저장/KST 응답 `createdAt` 을 반환하며 성공 응답은 `201` + `ApiResponse` wrapper 로 직렬화한다. 실패 케이스로 현재 사용자 식별자 null/blank 시 `INVALID_REQUEST(400)` 로 생성 차단하고 Repository 저장을 수행하지 않도록 했다. `Conversation` 문서에 `isPinned` 기본값과 `{userId, deletedAt, isPinned, lastMessageAt}` 복합 인덱스 정의를 반영했고, `backend/bff-server/current-plans.md` Feature 3 첫 체크박스만 `[x]` 처리. 검증: `./gradlew :bff-server:test --tests 'com.lina.bff.chat.service.ConversationServiceTest' --tests 'com.lina.bff.chat.controller.ConversationControllerTest' --tests 'com.lina.bff.chat.repository.ConversationRepositoryTest'` 통과.
- **Admin Key deactivate 책임 ML → BE 이동**: 회의(2026-06-02)에서 ML 담당으로 결정됐던 것을 BE 가 처리하는 게 깔끔하다는 판단으로 변경(2026-06-04). 구현 메커니즘: BFF 가 `/api/admin/ingest` 트리거 직후 Virtual Thread watcher 를 띄워 `/ml/ingest/status/{jobId}` 를 폴링(`lina.admin.ingest-watch-interval-ms`, 기본 30s) → `COMPLETED`/`FAILED` 감지 시 auth-server 내부 `POST /internal/admin/key/deactivate` 호출 → admin 의 OAuth access_token 으로 Atlassian admin-key deactivate API 호출. BFF 재시작 시 watcher 손실은 60분 TTL 이 fallback (영속 watcher 는 PoC 범위 밖). **ML 인터페이스 변경 없음** — `/ml/ingest` 응답·동작 동일, ML 팀에 별도 협의 불필요. 변경 파일: `docs/api-spec.md` §1-4 Admin Key 말소 노트(ML→BE)·§3 호출 흐름 다이어그램·changelog v2.4.0(추가 bullet), `docs/adr/0001-page-level-acl-source.md` §2.1 역할 분리(auth-server 에 deactivate 내부 API, BFF watcher 책임, ML deactivate 책임 제거), `backend/auth-server/current-plans.md` Feature A 에 deactivate 내부 API 체크리스트 행 신설, `backend/bff-server/current-plans.md` 4단계 placeholder `/api/admin/ingest` 내부 처리 ④ watcher 단계 추가.
- **`spaceKey` 전면 제거 — LINA API 표면에서 사용 안 함 (api-spec v2.4.0)**: 06-02 에 `/ml/query` 만 Optional 화한 결정의 연장. 실제 검토 결과 `/ml/ingest`·`/api/admin/ingest` 도 admin Key 로 admin 이 접근 가능한 모든 스페이스를 일괄 크롤하는 모델이라 spaceKey 가 불필요. 결과: (1) `/ml/query` request body 에서 `spaceKey` 필드 삭제 — cross-space 검색이 유일한 모드, ACL(`userId`/`groups`) 만 적용. (2) `/ml/ingest` request body 에서 `spaceKey` 필드 삭제 — ML 이 accessToken+cloudId+Admin Key 헤더로 접근 가능 스페이스 iterate. (3) `/api/admin/ingest` request body 가 `{ mode }` 로 축소(생략 시 `"full"`), 버튼 1회 = 전체 수집. (4) Common §스페이스 식별자 섹션에서 `spaceKey` 행 제거, `spaceId`/`spaceName` 만 유지(`messages.sources` 출처 표시용). spaceKey 는 Confluence URL/내부 식별자로만 존재한다는 노트 추가. (5) fail-closed 표현에서 `spaceKey` 잔존 제거 — `userId`/`groups` 만 게이트. ADR 0001 §2.5 의 과거 `space:{spaceKey}` 합성 토큰 모델은 폐기 노트 추가(페이지-단위 권한으로 자연스럽게 마이그레이션). 2단계 demo 영향: `lina.demo.fixed-space-key` 설정 deprecation 대상. **ML 팀 협의 필요** — `/ml/query`·`/ml/ingest` body schema 변경. 변경 파일: `docs/api-spec.md` (Common §스페이스 식별자·§1-4 `/api/admin/ingest` body·§2-1 `/ml/query` body+example+필드표·§2-1 fail-closed 노트·§2-2 `/ml/ingest` body+설명·버전 v2.3.0→v2.4.0·changelog v2.4.0 행 신설), `backend/rules/rag-pipeline.md` §2 검색 스코프 항목 제거, `backend/bff-server/current-plans.md` Feature 5 검색 컨텍스트·fail-closed 게이트·4단계 placeholder `/api/admin/ingest` 행, `docs/adr/0001-page-level-acl-source.md` §2.5 합성 토큰 폐기 노트.

## 2026-06-02

- **2026-06-02 회의 결정 3건 반영**: (1) **`/api/admin/ingest` 가 내부적으로 key activate 묶음 처리** — admin "데이터 인제스천 파이프라인" 버튼 하나로 키 발급+수집 일괄 트리거. BFF 가 key 활성 상태 확인 후 만료/미활성이면 자동 `POST /api/v2/admin-key` 호출 → ingest 진행. `/api/admin/key/activate` endpoint 는 **수동/테스트용**으로 명시(검증·디버깅·운영 점검). (2) **Admin Key 말소 책임 = ML(Data Ingestion)** — ingestion 완료 직후 ML 이 Atlassian admin-key deactivate 호출, 60분 TTL 은 fallback. BE 는 deactivate 책임 없음. ML 팀에 협의·통보 필요. (3) **`/api/auth/login?mode=admin` 쿼리 파라미터 도입** — FE "Continue with Confluence for Admin" 버튼이 `?mode=admin` 전달. BE 가 state 토큰에 mode 직렬화(서버 측 세션 또는 서명 state)해 callback 까지 전달(클라이언트 우회 방지). callback 에서 `state` 의 mode 가 admin 인데 `users.role != ADMIN` 이면 `403 FORBIDDEN`(message: "관리자 권한이 없는 계정입니다") 으로 차단, 토큰 미발급. 회의록의 "관리자 검색 사용 시 로그아웃 후 일반 로그인" 동선은 FE/UX 영역(BE 무관). 변경 파일: `docs/api-spec.md` §1-4 자격증명 모델 노트(key 활성화 시점/말소 책임 추가)·§1-4 Admin Key 활성화 sub-section 헤더(수동/테스트용 명시)·§3 호출 흐름 다이어그램·§4-1 `/api/auth/login` Query Parameter 표(mode 추가)·`/api/auth/callback` 응답 케이스(403)·changelog v2.3.0. `backend/auth-server/current-plans.md` Feature A login/callback 두 항목 mode 게이트 보강. `backend/bff-server/current-plans.md` 4단계 placeholder key/activate 와 ingest 행 갱신. `docs/adr/0001-page-level-acl-source.md` §2.1 역할 분리(BFF 자동 activate, ML deactivate 책임).
- **`/ml/query` `spaceKey` Required → Optional**: RAG 챗봇 UX 가 "사용자가 매번 스페이스를 고르지 않고 질문만 던지면 알아서 권한 가능한 모든 콘텐츠에서 답변" 이 자연스러움. 따라서 `spaceKey` 는 누락 시 cross-space 검색(`userId`/`groups` ACL 만 적용), 지정 시 특정 스페이스로 좁힘. **ACL fail-closed 게이트에서 `spaceKey` 제거** — ACL 이 아닌 스코프 필드이므로 누락이 차단 사유가 아님. `userId`/`groups` 만 fail-closed. `/ml/ingest`·`/api/admin/ingest` 는 admin 이 수집 대상을 명시해야 하므로 **`spaceKey` Required 유지**. 2단계 demo 는 기존 색인 데이터(`allowed_groups: ["space:CPC"]` 합성, ADR 0001 §1 mismatch)와의 매칭 유지를 위해 `lina.demo.fixed-space-key=CPC` 계속 전달. 3단계 이후 FE 가 스페이스 선택 UI 도입 시 사용자 미선택은 누락 처리. **ML 팀과 `/ml/query` body schema 변경 협의 필요**. 변경 파일: `docs/api-spec.md` (Common §spaceKey 정의·§2-1 필드 표 Required→— ·fail-closed 노트·Request Body 예시 보강 노트·changelog v2.3.0), `backend/rules/rag-pipeline.md` §2, `backend/bff-server/current-plans.md` Feature 5 (검색 컨텍스트·fail-closed 게이트 두 항목).
- **대화 검색 endpoint 신설 확정**: `GET /api/conversations/search?q=&page=&size=` (api-spec §1-2 신규 sub-section). 결정 사항: (1) **하이라이트 = plain snippet + `matchPositions: [[start, end]]`** — 서버는 `<mark>` 등 HTML 미생성, FE 가 인덱스로 마킹 (XSS 안전성 + 응답 sanitize 책임 단순화). 인덱스는 UTF-16 code unit, end exclusive. (2) **role 필터 옵션 미도입** — `user`/`assistant` 본문 모두 매칭(질문·답변 무관). 사용자가 "답변만" 원하는 케이스가 명확해지면 후속에 `?role=` 추가. (3) **`q` 검증** — required + trim 후 길이 2~50자, 위반 시 `400 INVALID_SEARCH_QUERY` (새로운 도메인 특화 errorCode 도입 — Common ErrorCode enum 첫 도메인 코드, 사용처 명확할 때만 추가하는 정책 노트 동반). 대화당 매칭 메시지 **최대 3개** + `matchCount` 동반. 정렬은 `lastMessageAt` DESC (PoC, 관련도 점수는 외부 검색엔진 도입 시). 권한: 본인 `userId` 의 대화만. PoC 구현은 MongoDB `$regex` (case-insensitive, 메타문자 escape) on `messages.content`, 후속 라운드에서 text index 전환 검토. 변경 파일: `docs/api-spec.md` (Common errorCode enum 확장·§1-2 sub-section 신설·§3 호출 흐름 다이어그램·changelog v2.3.0), `docs/db-schema.md` (§3.2 후속 text index 권고 행·변경 이력 행), `backend/bff-server/current-plans.md` (Feature 7 = 대화 검색 신설, 기존 검증 → Feature 8 로 번호 이동, Done Definition 카운트 갱신).
- **정합화 패스 — 2차 (1차 패스 후 추가 검증으로 잡힌 5건)**: (1) `backend/bff-server/current-plans.md` line 305 forward-looking 표기에서 `admins` 제거 + `users.role` 흡수 노트. (2) `backend/bff-server/current-plans.md` line 488 확정된 결정 #4(데이터 저장소) 동일 갱신. (3) `docs/architecture.md` line 37 컴포넌트 표 MySQL 행에서 "관리자 등" 표기를 "`role` 컬럼으로 USER/ADMIN 구분"으로 교체. (4) `docs/api-spec.md` line 779 공통 JWT claim 리스트에 `role`(`USER`/`ADMIN`) 추가 + accountId 노트 + `/api/admin/*` role 추가 검사 명시. (5) `backend/auth-server/current-plans.md` Feature C line 77 JWT Claim 리스트에 `role` 추가. (6) `docs/api-spec.md` §3 호출 흐름 다이어그램에 `POST /api/admin/key/activate` 추가(2단계 섹션의 ingest 위).
- **정합화 패스 (admin 흡수·role claim·userId vocabulary 파급 부정합 5건)**: (1) `backend/CLAUDE.md` §2.2 MySQL 3단계 목록에서 `admins` 표기 제거(괄호 안에 `users.role` 흡수 노트). (2) `docs/architecture.md` §6.1 MySQL 저장 대상 목록에서 별도 `관리자 (admins)` 줄 제거, `users` 줄에 "role 컬럼 = JWT role claim source" 보강. (3) `backend/auth-server/CLAUDE.md` §1 JWT Claim 리스트에 `role` 추가 + `userId` = accountId 노트. (4) `backend/rules/auth.md` §2 JWT Claim 리스트에 동일 보강. (5) `docs/adr/0001-page-level-acl-source.md` §4 #1 (vocabulary open question) 갱신 — `userId = accountId` 는 `users.user_id` 정의로 확정(2026-06-02), 남은 open 은 `groups` vocabulary (group `name` vs `groupId`) 만.
- **role 결정 = MySQL `users.role` DB 단일 source**: 로그인 페이지에 "사용자 로그인" / "admin 로그인" 두 버튼이 생기지만 OAuth 흐름은 동일하고 role 은 백엔드가 DB 에서 결정해 JWT claim 으로 발급. YAML bootstrap config 안 씀(런타임 분기 없음·single source of truth). 최초 admin 은 첫 배포 마이그레이션 스크립트(`V001__seed_initial_admin.sql`)에 accountId 를 **하드코딩 INSERT** (`role='ADMIN'`). 추가 admin 부여/박탈은 DB UPDATE 또는 향후 admin 관리 API. 별도 `admins` 테이블 계획은 `users.role` 컬럼으로 흡수. 변경: `docs/db-schema.md` §1·신규 §6.1 `users` 테이블 정의·변경 이력 행, `backend/auth-server/current-plans.md` Feature A 에 OAuth callback users upsert + seed 항목·확정된 결정 행, `docs/api-spec.md` §4-1 `users/me` `role` 노트.
- **admin ingestion 자격증명 모델 확정: OAuth + Admin Key 단일 자격증명**: 팀 Confluence Admin Key 테스트(`confluence_admin_key_test_summary.md`, 2026-06-02) 결과 반영. admin 도 일반 사용자와 동일하게 Confluence OAuth 3LO 로 로그인하고(저장된 OAuth access_token 그대로 사용), ingestion 자격증명도 같은 admin OAuth 토큰 + Atlassian `Atl-Confluence-With-Admin-Key: true` 헤더 조합으로 결정 — 별도 API Token 미사용. UI 흐름: admin 로그인 → "Admin Key 발급" 버튼 → BFF `POST /api/admin/key/activate` → auth-server 가 admin OAuth 토큰으로 Atlassian `POST /api/v2/admin-key` 호출(60분 활성화) → admin 이 "수집 시작" → `/ml/ingest`. **검증 게이트**: 팀 테스트는 API Token (Basic auth) 기반이라 OAuth Bearer + Admin Key 헤더 조합은 3단계 auth-server 구현 직후 curl 로 검증, 실패 시 admin API Token 별도 보관 모델로 전환(plan 한 행 정정 fallback). 변경 파일: `docs/adr/0001-page-level-acl-source.md`(Proposed → **Accepted**, §2.1 구현 경로 구체화 + §4 미해결 갱신), `docs/api-spec.md` §1-4(Admin Key 활성화 endpoint 신설·자격증명 모델 노트), §2-2(`accessToken` 시맨틱 명확화), changelog v2.3.0, `backend/auth-server/current-plans.md`(Feature A 에 활성화·검증 게이트 + 확정된 결정 행 추가), `backend/bff-server/current-plans.md` 4단계 placeholder(Admin Key 활성화 endpoint 추적 추가).
- **데이터 수집 모델 확정: admin-only**: 사용자 onboarding 모델(per-user OAuth → 본인 ingest) vs admin-only 시스템 ingest(query-time ACL) 사이에서 **admin-only 확정**. 근거: 기획서 §6.6 의 "단일 인덱스 + 사용자 단위 검색"(query-time ACL) 디자인과 정합, 중복 수집 회피, PoC 구현 단순. 결과적으로 (1) `docs/api-spec.md` §1-4 `POST /api/admin/ingest` 의 ADMIN 권한이 그대로 유지, (2) 별도 endpoint(`/api/ingest/request`) 도입 안 함, (3) 팀에서 받은 **ONBOARDING-01(상태) / ONBOARDING-02(취소) PDF 는 폐기/out-of-scope**(필요 시 admin 대시보드 모니터링 화면으로 재해석은 FE 디자인 영역).
- **bff-server/current-plans 정합화 3건**: (1) Feature 6 피드백 `rating(like/dislike)` 오타 → `LIKE`/`DISLIKE` UPPER (api-spec §1-3·Enum 정책 정합). (2) 4단계 placeholder 에 누락돼 있던 `POST /api/admin/ingest`·`GET /api/admin/ingest/status/{jobId}` 를 "데이터 수집 트리거 (관리자용)" sub-section 으로 추가(api-spec §1-4·§2-2 정합, 2단계 demo 데이터 셋업 용도도 명시). (3) Feature 4 메시지 이력 응답 필드에 `role(user/assistant, lowercase)` 명시 보강.
- **Confluence OAuth 토큰 DB 저장 시점 확정: callback 즉시 영속**: `backend/auth-server/current-plans.md` Feature A 와 작업 요약 모드 표 갱신. 기존엔 "PoC=메모리/세션, 확장=MySQL 암호화"로 저장 시점이 모드별로 갈렸으나, admin-only ingest 가 admin 의 세션·서버 재시작 무관하게 토큰을 써야 하므로 **PoC 부터 MySQL 암호화 저장으로 일원화**. 모드 구분은 이제 **전송 방식**(PoC=accessToken/cloudId 직접 전달 / 확장=connectionId+cloudId)로만 남음. 확정된 결정 표의 "Token 암호화 방식 TBD" 행을 "PoC 부터 MySQL 암호화 저장" 으로 갱신, Rotating Refresh 노트의 저장소 분기 표현도 단일화.

---

## 2026-06-01

- **§3 호출 흐름 다이어그램 확장**: "전체 호출 흐름"이라는 제목에 맞게 §4(인증·관리자 대시보드·미리보기)까지 포함하도록 그룹별로 재구성(2단계 / §4-1 / §4-2 / §4-3). 기존 "§4는 범위 밖" 안내는 제거 — 구현 전 명세 완성 방침에 따라 진짜 '전체' 다이어그램으로 통합. `docs/api-spec.md` §3.
- **`messageCount` 제거**: §1-2 대화 목록 응답의 `messageCount` 필드 제거. 기획서 §5.2/§6.7 에 근거 없고(채팅 사이드바에서 카운트 노출 요구 없음), db-schema 에도 정의 안 됨 → 비정규화/집계 비용 없이 단순화. 필요해지면 그때 재도입.
- **db-schema 시간 표기 행 정정**: `docs/db-schema.md` §2 "시간 필드" 행이 "API ISO-8601 `Z` 표기와 일치"라고 stale 문구를 갖고 있던 것을 KST 정책(2026-05-21 확정)에 맞게 "응답 JSON 은 KST(`+09:00`)" 로 정정. api-spec ↔ db-schema 시간 표기 일관.
- **§2-1 RAG 질의 입력 명세 정밀화**: RAG 팀 협의 결과 반영. (1) 엔드포인트는 `POST /ml/query` 하나, 요청/응답을 동일 절에서 명시적으로 분리 표기. (2) Request Header 표·Request Body 필드 표(Required) 정형화. (3) **`stream` 필드 추가**(기본 `false`이지만 BFF 는 항상 `true` 로 호출 — 토큰 스트리밍, PoC fallback 가능). (4) **`history[].role` 을 RAG/OpenAI 관용 소문자**(`user`/`assistant`)로 매핑. (5) `groups`/`spaceKey` **fail-closed** 명문화(BFF 가 빈 값 시 호출 차단). (6) **boundary 변환 명시**: RAG `error` `code` → BFF `errorCode`, RAG `done` `{}` → BFF 가 `messageId` 채워 중계.
- **SSE `error` 키 매핑 노트 정정 (ML팀 spec 재확인)**: 위 §2-1 정밀화·Feature 5 보강·rag-pipeline 정합화 항목들에서 "RAG `code` → BFF `errorCode` 매핑" 으로 기록했던 boundary 변환은 ML팀 spec의 generic placeholder(`{ "code": "...", "message": "..." }`) 를 잘못 읽은 결과. **실제로는 RAG·BFF·FE 모두 `errorCode` 단일 키 동일** 이라 BFF 는 키 매핑 없이 passthrough(값이 §1-1 에러 코드 표 일치 여부만 검증). `done: {}` → `messageId` 채움은 유효(BFF가 DB 저장 후 생성). 정정 파일: `docs/api-spec.md` §2-1·changelog v2.3.0, `backend/rules/rag-pipeline.md` §3, `backend/bff-server/current-plans.md` Feature 5.
- **§4 신규 endpoint plan 추적 보강**: api-spec §4 의 신규 endpoint 중 plan 미추적 항목을 보강. (1) `backend/auth-server/current-plans.md` Feature A 에 `POST /api/auth/logout`·`GET /api/users/me` 항목 추가(이전엔 login/callback/refresh만). (2) `backend/bff-server/current-plans.md` 에 **`# 4단계 — 부가 API`** 섹션 신설(placeholder) — §4-2 관리자 대시보드 5개 endpoint·§4-3 미리보기 1개의 엔드포인트·응답 핵심·데이터 소스·권한·기획서 매핑을 표로 정리, 6주차 진입 시 풀세트 Feature 로 확장하도록 명시. 진행 상태 요약 표의 4단계 description 에 "Confluence 미리보기" 도 추가.
- **`backend/bff-server/current-plans.md` Feature 5 보강**: api-spec §2-1 정밀화 결과를 체크리스트로 끌어옴 — (1) ML 호출 전달 항목에 `stream: true`(BFF 항상 명시)·`history[].role` lowercase 그대로 전달(변환 없음) 추가, (2) `groups`/`spaceKey` 빈 값 시 호출 차단하는 **ACL fail-closed 게이트** 항목 신설, (3) **Boundary 변환** 항목(`code`→`errorCode` 매핑·`done {}` → `messageId` 채움) 신설. 구현자가 체크리스트만 보고도 누락 없이 만들도록 명시.
- **`backend/rules/rag-pipeline.md` 정합화**: §2 "요청 시 전달 항목" 에 `spaceKey`·`stream: true` 추가, `history[].role` lowercase·fail-closed 명시. §3 "응답 처리" 의 stale 한 "MySQL에 저장" → **MongoDB** 로 정정하고 user 선저장/assistant `done` 시 저장/`error` 시 미저장 규칙 명문화. RAG → BFF → FE **boundary 변환**(`code`→`errorCode`, `done: {}` → `messageId` 채워 중계) 추가 — api-spec §2-1·§1-1 과 정합.
- **메시지 `role` 저장 표기 lowercase 통일**: 위 §2-1 정밀화에서 "BFF boundary 변환(USER↔user)" 로 잡았던 것을 한 단계 더 정리. **저장(`messages.role`)·외부 응답(`§1-2`)·RAG 와이어(`/ml/query history[].role`) 모두 lowercase `user`/`assistant`** 로 통일 — LLM/OpenAI 산업 표준, RAG boundary 매핑 자체를 제거(더 단순). `rating`(LIKE/DISLIKE)·`verificationResult`·`status`·사용자 `role`(USER/ADMIN) 등 다른 UPPER enum 은 그대로 유지. Common Enum 정책의 lowercase 예외로 명시. 변경 파일: `docs/api-spec.md`(Common·§1-2·§2-1·changelog), `docs/db-schema.md`(필드·샘플·변경이력), `backend/bff-server/current-plans.md`, `backend/bff-server/.../entity/MessageRole.java`(enum 상수 lowercase + javadoc), `Message.java`(javadoc), `MessageRepositoryTest.java`(테스트 참조).

## 2026-05-29

- **페이지 단위 ACL 권한 모델 협의 (ADR 0001 초안)**: RAG 팀의 "페이지/유저 단위 권한 전환" 요청에 대응. **source of truth = Confluence content restrictions API(수집 단계)** 로 결정, BE 는 별도 권한 테이블 없이 (a) 수집용 OAuth 토큰 제공 (b) 질의 시 JWT claim 발급만 담당. 질의 측은 **JWT pass-through**(식별자 값 무변형), 매칭 책임은 색인(Ingestion)이 payload 를 JWT vocabulary 에 맞추는 쪽으로 둠. 신규 파일: `docs/adr/0001-page-level-acl-source.md` (상태: Proposed — RAG/ML 합의 대기). 미해결: JWT vocabulary 확정, inherited 권한 산출 책임, 첨부파일 권한 상속, `/ml/query` 무-Confluence 전제 재확인 (ADR §4).
- **JWT claim·내부 API 필드 표기 통일 (`user_id` → `userId`)**: JSON 표면 전체가 camelCase(`conversationId` 등)인데 JWT claim 만 snake 라 혼재. claim·`/ml/query` 와이어 필드를 모두 **camelCase `userId`/`groups`** 로 통일. Qdrant payload 키(`allowed_users`/`allowed_groups`)는 ML/Qdrant 표기 유지, DB 컬럼(snake)은 불변. 변경: `backend/rules/auth.md`(정의+근거)·`domains.md`·`rag-pipeline.md`, `backend/auth-server/CLAUDE.md`·`current-plans.md`, `backend/bff-server/current-plans.md`, `docs/api-spec.md` §2-1, `docs/adr/0001`, `application.yml` 주석, `Conversation.java` javadoc.

### api-spec 정합성 리팩토링 (branch `refactor/#4/api-spec`)

- **채팅방 고정(`isPinned`) 풀세트 정합화**: 목록 응답에만 있던 고아 필드를 FE 고정 기능에 맞춰 3개 문서로 정합. `PATCH /api/conversations/{id}` 를 제목/`isPinned` 부분 수정으로 확장(전용 엔드포인트 대신 기존 PATCH 확장 채택), 목록은 **고정 우선 → 최신순** 정렬. 변경: `docs/api-spec.md` §1-2, `docs/db-schema.md`(`conversations.isPinned` + `idx_conversations_user_active_recent` 에 `isPinned:-1` 추가), `backend/bff-server/current-plans.md` Feature 3. 코드(엔티티/Repository/컨트롤러)는 Feature 3 미착수라 체크리스트로 추적.
- **Enum 값 표기 정책 확정 + `role`/`rating` 모순 해소**: api-spec 이 자기모순(`users/me` `role:"USER"` vs 메시지 `role:"user"`)이었고 db-schema·current-plans·`verificationResult`·`status` 는 전부 UPPER. **도메인/저장 enum 값 = `UPPER_SNAKE`** 로 정책 확정(`docs/api-spec.md` Common 에 명문화), api-spec 의 소문자 `role`/`rating` 을 `USER`/`ASSISTANT`·`LIKE`/`DISLIKE` 로 정정. 예외: `mode`(full/delta)·SSE `phase`(lower_snake). 필드 *이름* camelCase 규칙과는 별개.
- **SSE 이벤트 계약 정본화 + 종료/영속/에러 명문화**: 같은 SSE 이벤트 집합이 4개 문서에서 불일치(규칙·계획 문서가 `status`/`meta` 누락, §2-1 이 `error` 누락). `docs/api-spec.md` §1-1 을 **7종 정본**으로 삼고 §2-1·`backend/rules/rag-pipeline.md` §4·`current-plans` Feature 5 를 정렬. 스트림은 `done`/`error` **정확히 하나**로 종료, user 선저장→`done` 시 assistant 저장(`error` 시 미저장·`messageId` 없음), 0건은 `sources` 빈 배열+`done`+`messageId` 정상 종료를 명문화. error payload `code`→**`errorCode`**(Wrapper `errorCode` 체계와 정합) + 에러코드 enum(`ML_SERVER_ERROR`/`ML_TIMEOUT`/`ML_CONNECTION_ERROR`) 신설. **추가(G~J)**: 이벤트 순서 불변식(status→`token` 연속→`sources`→`verification`→`meta`→`done`), **idle 기준 타임아웃 + `status` keep-alive**, SSE 응답 헤더(`text/event-stream`/`no-cache`/`X-Accel-Buffering: no`), **재연결 미지원**(`Last-Event-ID` 없음·비멱등), `meta` 타이밍 '즈음'→'직전 1회' 정정까지 명문화 — SSE 미해결 이슈 전부 해소. `backend/rules/rag-pipeline.md` §4·`current-plans` F5 동반 정렬.
- **에러 봉투 · `stream` · 자동 제목 규칙 (A·F·B)**: (A) 에러 응답을 구현 `common/ErrorResponse`(`{isSuccess,code,errorCode,message}`·`data` 없음)에 맞춰 §4-3 미리보기 에러 예시 정정 + Common 에 **에러 4필드 고정** 규칙·`ErrorCode` enum 6종 명시. (F) 정의 없는 `stream=false` 댕글링 제거 → "이 엔드포인트는 항상 SSE 스트리밍, 비-스트리밍 모드 없음" 으로 정정. (B) `meta.title` → 대화 제목 **첫 assistant 응답 시 1회 자동 설정**(현재 `title` 이 기본 `"새 대화"` 일 때만, 이후·사용자 PATCH 수정 우선) 규칙 명문화 + `current-plans` F5 항목 추가.
- **스페이스 식별자 · `feature13` · 변경 이력 (C·D·E)**: (C) `spaceKey`(고유 키·스코프) / `spaceId`(내부 ID·출처) / `spaceName`(표시 라벨) 3종 구분을 Common 에 명시(주: 2단계 단일 고정 스페이스에선 사실상 무의미, 멀티 스페이스 3단계+에서 유효 — 실제 단순화 후보는 `spaceName` 이 아니라 `spaceKey`↔`spaceId`). (D) 미정의 마커 `feature13` → 서술형("ML 응답을 BE 통합 목표 계약으로 마이그레이션 이후")으로 교체. (E) `## 변경 이력`(v2.2.0) 신설 + "문서 자체 버전 ≠ 기획서 버전" 명시. 변경 이력 위치를 **헤더 직후 상단**으로 옮기고 `docs/db-schema.md` 도 동일하게 통일(§6 → 상단 무번호 섹션). 추가로 §4-3 미리보기 쿼리 파라미터 `page_id`→`pageId` 로 camelCase 정합(다른 모든 곳은 `pageId`).
- **§4(5~7주차) 미래 엔드포인트 명세 작성 (기획서 기반)**: "구현 전 명세 완성" 방침에 따라 비어 있던 §4 를 채움. **§4-1 인증** — login/callback/refresh/logout/me 의 요청·응답·에러를 **FE-facing 계약**으로 작성(기획서 §6.5). **세션은 `Authorization: Bearer` JWT 방식(HttpOnly 쿠키 미사용)** — 로그인/갱신 응답으로 access+refresh 토큰 발급, Confluence OAuth 토큰은 서버 보관. JWT 서명·access TTL·Refresh 저장은 `TBD(3단계)`. **§4-2 관리자 대시보드** — 누락 `GET /api/admin/feedback` 응답 신설(긍정/부정·추이·부정 원문, QCA 로 question/answer 매핑), `users` 에 접근 가능 스페이스/페이지/첨부 수 보강, ADMIN 전용(미인증 401·일반 403)·공통 쿼리 파라미터(`period`/`from`/`to`/`page`/`size`, **제안**) 명시(기획서 §6.7·`domains.md` §4). 기존 요약 표는 보존하고 상세 서브섹션 추가.
- **auth-server 외부 OAuth 계약 정리**: 팀 사전 노트(AUTH-01~04, Notion export)를 `backend/auth-server/current-plans.md` 에 **§외부 OAuth 계약 참조 (Atlassian 3LO)** 로 캡처. authorize / token 교환 / refresh / accessible-resources 는 **Atlassian 제공자 엔드포인트**(우리 API 아님 — `api-spec §4-1` FE 계약과 혼동 금지)임을 명시. `offline_access` 필수, **Rotating Refresh Token**(저장소 덮어쓰기·재사용 금지), cloudId → `…/ex/confluence/{cloudId}/…` URL 조합, `invalid_grant` 에러. **scope 충분성(summary scope 로 RAG 본문 수집 부족 가능)은 검증 TODO**. Feature A 에 `POST /api/auth/refresh` 항목 추가. 출처 노트는 "초안·검증 필요"로 표기.
- **세션 전달 방식 결정 (HttpOnly 쿠키 → `Authorization: Bearer`)**: 사용자 결정으로 세션 JWT 를 쿠키가 아니라 **Bearer 헤더**로 전달. 로그인/갱신 응답 `data` 로 access+refresh 발급(FE 보관), **Confluence OAuth 토큰은 FE 비노출·서버 보관**. `api-spec` §4-1(callback→JSON, refresh Body, logout)·Common 헤더, `auth.md` §3, `auth-server/CLAUDE.md`·`current-plans.md` Feature C 일괄 정정.
- **관리자 권한 보완 + 출처 첨부 파일명 제외 (리뷰 #3, #1)**: (#3) `/api/admin/ingest`(§1-4)에 ADMIN 전용(401/403) 권한 노트 추가(§4-2 정합). (#1) `sources[].attachmentName` 은 잠시 추가했다가 **제외 결정** — FE 가 첨부 파일명을 표시할 필요 없음(2026-05-29). api-spec·db-schema 에서 되돌리고, `domains.md` §1 출처 정보에서도 첨부 파일명을 제거해 정합(기획서 §6.4 는 언급하나 PoC 계약 미포함).

## 2026-05-22

- **Confluence 토큰 전달 대상 변경 (`/ml/query` → `/ml/ingest`)**: 토큰(`accessToken`/`cloudId`) 을 RAG 질의 본문에서 제거하고 Data Ingestion 호출 본문에만 첨부하도록 일괄 갱신. **근거**: 권한은 수집 시 Qdrant payload(`allowed_groups`/`allowed_users`) 에 ACL 저장 + 질의 시 JWT 의 `user_id`/`groups` 로 필터링 (기획서 §6.4/§6.6). 따라서 `/ml/query` 는 라이브 Confluence 호출이 없어 토큰 불필요. **전제**: `/ml/query` 가 실시간 Confluence 호출을 일절 안 한다고 가정 (※ ML 확인 대기). 변경 파일: `docs/api-spec.md` §2-1/§2-2 (토큰 필드·보안 박스 이동), `backend/bff-server/current-plans.md` 확정 결정 #5 + Feature 5 체크리스트.
- **Data Ingestion 클라이언트 키 정합화**: `bff-server` 설정·빈 이름을 그림(아키텍처)의 박스명과 1:1 로 일치시킴 — `application.yml` 의 두 번째 ML 클라이언트 블록 이름을 `data-ingestion` 으로 명확화 (관련 env: `DATA_INGESTION_BASE_URL`/`DATA_INGESTION_TIMEOUT_MS`), `RagClientConfig` 의 두 번째 RestClient 빈 이름을 `dataIngestionRestClient` 로 정렬, 테스트(`RagClientConfigTest`) 검증 빈 이름·DisplayName 동기화. base-url fallback 동작은 기존과 동일(`${ML_PIPELINE_BASE_URL:}` 폴백 유지) — 이름만 변경. 빈은 4단계 `/api/admin/ingest` 도입 전까지 미사용 placeholder.

## 2026-05-21

- **팀 피드백 수신·문서 반영**: 3가지 사항을 관련 문서에 반영.
  - **Confluence OAuth → ML 서버 토큰 전달**: 3단계에서 **PoC 모드(`accessToken` + `cloudId` 직접 전달) 우선** 구현, 후속 라운드에서 DB 기반 `connectionId` 로 확장 (`backend/auth-server/current-plans.md` §3단계 Feature A~D, 확정된 결정 #5). ※ 토큰 전달 대상 엔드포인트는 2026-05-22 자로 `/ml/ingest` 로 조정 (당일 working-log 항목 참조).
  - **응답 timestamp 정책**: 저장은 UTC, 응답 JSON 은 KST(`+09:00`) 절대 전환. `docs/api-spec.md` 응답 예시 15건 일괄 갱신, bff-server Feature 3/4/5/6 체크리스트에 KST 직렬화 항목 추가 (확정된 결정 #6).
  - **ML 클라이언트 / Confluence 설정 키 추가**: `bff-server/application.yml` 에 두 번째 ML 클라이언트 블록 및 `lina.confluence.*` 환경변수 참조 추가 (평문 secret 없음). ※ 두 번째 ML 클라이언트 블록 이름은 2026-05-22 자로 Data Ingestion 클라이언트로 정합화 (당일 항목 참조).
- **보안 운영 규칙 합의**: PoC 모드 한정 — 로그·tracing 본문 마스킹, RabbitMQ payload 토큰 미포함, actuator `env`/`heapdump` 비노출, ML Pod NetworkPolicy 적용. `docs/api-spec.md` 보안 주의 박스에 명시 (대상 엔드포인트는 2026-05-22 자 항목 참조).
- **Health check 범위 확인**: BFF 자체 `/actuator/health` 이미 노출, ML 서버 health 는 `docs/api-spec.md` §2-4 정의 + Resilience4j 격리 정책 유지. Data Ingestion / RAG 세부 indicator 는 영훈·태성 담당으로 분리.

## 2026-05-20

- **저장소 전환**: 2단계 BFF 도메인 영속을 MySQL/JPA → MongoDB/Spring Data MongoDB 로 전환. `message_sources` 별도 컬렉션 → `messages.sources` 내장 배열. `backend/CLAUDE.md` §2.2/§6/§7, `docs/db-schema.md`, `docs/architecture.md` §3/§6, `backend/bff-server/current-plans.md` 확정된 결정 #4 갱신.
- **디렉토리 구조 정리**: Gradle 멀티모듈 root 를 `backend/` 하위로 이동. 모듈별 `CLAUDE.md` / `current-plans.md` 분리. `docs/ai/working-log.md`, `docs/adr/` 신규 도입. `docs/ai/current-plan.md` → `backend/bff-server/current-plans.md` 로 이관.

## 2026-05-19

- **2단계 Feature 1 완료**: 영속 계층 + DB 스키마 초안. 처음에는 MySQL/JPA + H2 로 구현 (테스트 7건 통과). 인증 부재 격리·데모 사용자 설정값(`lina.demo.*`) 도입.
- **2단계 Plan 작성**: bff-server 핵심 API 의 Feature 1~7 체크리스트와 인증 부재 처리 방침(`DemoSecurityConfig` + `CurrentUserProvider` 격리) 확정. 피드백 upsert(메시지당 1건, 신규 201/갱신 200), soft delete, 데모 사용자 설정값 결정.
- **`docs/api-spec.md` §4-3 추가**: Confluence 페이지 미리보기 (5주차 이후, 인증 의존, 호출 주체 TBD).

## 2026-05-15

- **1단계 완료**: Gradle 멀티모듈 골격(`common`, `bff-server`, `auth-server`), 공통 `ApiResponse`/`ErrorCode`/`GlobalExceptionHandler` 도입. checkstyle + spotless(google-java-format) 적용.
